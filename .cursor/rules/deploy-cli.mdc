---
description: 部署 CLI 规范（每应用自包含 deploy/）
globs: ["**/deploy/**/*"]
alwaysApply: false
---

# 部署 CLI 规范

## 原则

- **每应用一套完整 `deploy/`**，compose 内含 Postgres，**无全局 `ams` CLI**。
- 仓库根 **`deploy/` 不再承载共享 infra**（见 `app-self-contained.mdc`）。

## 各应用 CLI

| app_key | 应用根 | deploy 目录 | 命令 |
|---------|--------|-------------|------|
| `main` | `menu-master/` | `menu-master/deploy/` | **`ams-main`** |
| `novel` | `novel-sub/` | `novel-sub/deploy/` | **`ams-novel`** |

首次：`cd {app}/deploy && npm link`  
未 link：`node {app}/deploy/scripts/run.mjs <命令>`

## 命令表

| 命令 | 作用 |
|------|------|
| **`{cli} local`** | 启动本应用完整 Docker 栈 |
| **`{cli} local:infra`** | 仅 Postgres |
| **`{cli} local:frontend`** | 仅主应用：重建前端容器 |
| **`{cli} local:reset`** | 清库重建 |
| **`{cli} local:down`** | 停止本栈 |
| `{cli} help` | 帮助 |

## 配置分层

| 场景 | 可提交 | gitignore |
|------|--------|-----------|
| 本地 Docker | `{app}/deploy/config/.env.local` | `{app}/backend/.env` |

端口/库名以 [`app-registry.mdc`](./app-registry.mdc) 为准。

## 禁止

- 根级共享 infra compose 作为应用默认启动方式
- 文档只写 bash、Windows 无法执行的步骤
