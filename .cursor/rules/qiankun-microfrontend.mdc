---
description: Qiankun 微前端集成规范
globs: "{**/frontend/**,**/qiankun/**}"
alwaysApply: false
---

# Qiankun 微前端规范

**新子应用接入必读**：[`subapp-onboarding.mdc`](./subapp-onboarding.mdc)

## 架构原则

- 主应用为基座：菜单、路由、子应用容器。
- 一级菜单对应一个子应用；通过 `microapp_name` 绑定。
- 子应用可独立开发、构建、部署（本项目为 Vue 3 + Element Plus）。

## 子应用注册

从菜单 API 动态注册：

| 菜单字段 | Qiankun 配置 |
|---------|-------------|
| `microapp_name` | `name` |
| `entry` | `entry`（dev：`http://localhost:{Vite端口}`） |
| `route_prefix` | `activeRule`（如 `/media/${route_prefix}`） |

主库 `subapp_registry` + 环境变量 `SUBAPP_*_ENTRY`；前端 fallback 见主应用 `frontend/src/qiankun/`。

## 子应用 API（关键）

- Qiankun 在**浏览器**运行；子应用 **不得**用相对 `/api` 访问本子应用 BFF。
- `{sub}/frontend/src/services/apiConfig.js` 在 `__POWERED_BY_QIANKUN__` 时使用绝对 URL `http://localhost:{API端口}/api`。

## 生命周期

导出 `bootstrap`、`mount`、`unmount`（`vite-plugin-qiankun` 或手动）。

## 路由与 basename

- 子应用 Router `base` 从 `props.basename` 获取。
- 主应用：`/media/*` → `#subapp-container`。

## entry

- **浏览器** fetch entry，非容器内 fetch。
- dev：`http://localhost:{Vite端口}`（与 registry、compose 宿主机端口一致）。
- prod：Nginx `/subapps/{microapp_name}/`。
- **禁止** entry 使用 `host.docker.internal`。

## 嵌入态 UI

- `__POWERED_BY_QIANKUN__` 时隐藏子应用全站 Header，仅保留页内导航。

## 通信

- 主 → 子：Qiankun `props`（`basename`、`menuData` 等）。
- 跨应用：全局事件或 Qiankun 全局状态；unmount 时清理监听。

## 相关规则

`subapp-onboarding.mdc` · `app-registry.mdc` · `vue-web.mdc` · `egg-backend.mdc`
