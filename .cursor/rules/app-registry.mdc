---
description: 多应用并行开发时的应用名、端口、数据库命名注册表（自包含应用）
globs: "{menu-master/**,novel-sub/**,docs-project/**,**/deploy/**}"
alwaysApply: false
---

# 应用端口与命名注册表

每个应用**自带 Postgres/Redis**，并行运行时须使用**不同宿主机端口**。新增应用须先登记再写代码。

**人类可读副本**：[`docs-project/应用端口与命名注册表.md`](../../docs-project/应用端口与命名注册表.md)

## 分配原则

| 维度 | 规则 |
|------|------|
| 应用标识 `app_key` | 小写英文，全局唯一 |
| 应用根目录 | **完整项目目录**（见 `app-self-contained.mdc`） |
| API 端口 | 7001 起递增 |
| Agent 端口 | 随所属应用登记（非独立 app_key） |
| Vite dev | 5173 起递增 |
| Postgres 宿主机端口 | 5432 起递增（容器内仍为 5432） |
| Redis 宿主机端口 | 6379 起递增（容器内仍为 6379） |
| 数据库名 | `{app_key}_db` 或语义名 |
| 容器名 | `ams-{role}-{app_key}` |
| CLI | `ams-{app_key}`，位于 `{app}/deploy/` |

## 已登记应用

| app_key | 应用根目录 | CLI | API | Agent | Vite | PG 宿主机 | Redis 宿主机 | 库名 |
|---------|-----------|-----|-----|-------|------|-----------|--------------|------|
| `main` | `menu-master/` | `ams-main` | **7001** | — | **5173** | **5432** | **6379** | `admin_platform` |
| `novel` | `novel-sub/` | `ams-novel` | **7002** | **7003** | **5174** | **5433** | **6380** | `novel_db` |

## 标准目录（每个应用）

```
{app}/
├── frontend/
├── backend/
├── agent/                 # 可选；需要 Pi 的子应用必有
├── database/              # init.sql（可与 backend/database 合并，但须在本应用内）
├── workspace-templates/   # 有 agent 时
├── workspaces/            # gitignore
├── data/                  # gitignore
├── deploy/
│   ├── docker-compose.yml # 含 postgres、redis（可选）、api、frontend、agent
│   └── bin/ams-{app_key}
└── README.md
```

## 禁止

- 仓库根 `deploy/compose/infra.yml` 作为默认依赖
- `apps/` 下放置半套服务或独立 Agent 项目
- 硬编码端口；须读 env 或 `deploy/config/.env.local`

## Nginx / Qiankun（集成时）

| app_key | microapp_name | activeRule 示例 | dev entry |
|---------|---------------|-----------------|-----------|
| `novel` | `novel-app` | `/media/novel` | `//localhost:5174` |

主应用通过菜单注册子应用 entry；**子应用独立 compose 仍可单独运行**。
