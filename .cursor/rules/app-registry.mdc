---
description: 多应用并行开发时的应用名、端口、数据库命名注册表
globs: "{menu-master/**,project-sub/**,docs-project/**,**/deploy/**}"
alwaysApply: false
---

# 应用端口与命名注册表

每个应用**自带 Postgres**，并行运行时须使用**不同宿主机端口**。缓存默认 **memory**（见 `cache-local.mdc`）。**新增应用须先登记再写代码。**

**人类可读副本**：[`docs-project/应用端口与命名注册表.md`](../../docs-project/应用端口与命名注册表.md)

## 分配原则

| 维度 | 规则 |
|------|------|
| `app_key` | 小写英文，全局唯一 |
| 应用根目录 | 完整项目目录（见 `app-self-contained.mdc`） |
| API 端口 | 7001 起递增 |
| Vite dev | 5173 起递增 |
| Postgres 宿主机端口 | 5432 起递增（容器内仍为 5432） |
| 数据库名 | 语义名或 `{app_key}_db` |
| 容器名 | `ams-{role}-{app_key}` |
| CLI | `ams-{app_key}`，位于 `{app}/deploy/` |

## 已登记应用

见 [`docs-project/应用端口与命名注册表.md`](../../docs-project/应用端口与命名注册表.md)。实现时**禁止硬编码端口**，须读 env 或 `deploy/config/.env.local`。

## 标准目录

```
{app}/
├── frontend/
├── backend/
├── database/
├── deploy/
│   ├── docker-compose.yml
│   └── bin/ams-{app_key}
└── README.md
```

## Qiankun 集成字段

| 字段 | 用途 |
|------|------|
| `microapp_name` | Qiankun `name` |
| `route_prefix` | `activeRule` / basename |
| `entry` / `entry_dev` | 浏览器拉取的 dev/prod entry |

主库 `subapp_registry` + 菜单 API 返回 `entry`；环境变量 `SUBAPP_{KEY}_ENTRY`、`VITE_SUBAPP_{KEY}_ENTRY` 可覆盖。

详见 `subapp-onboarding.mdc`、`qiankun-microfrontend.mdc`。

## 禁止

- 仓库根 `deploy/compose/infra.yml` 作为默认依赖
- `apps/` 下放置半套服务
- 本地默认启动 Redis 容器
