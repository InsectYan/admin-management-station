---
description: 多应用并行开发时的应用名、端口、数据库命名注册表（主应用 + 子应用 + Agent）
globs: "{apps/**,menu-master/**,deploy/**,docker/**,docs-project/**}"
alwaysApply: false
---

# 应用端口与命名注册表

主应用、多个子应用与 Agent **同时运行**时，必须为每个应用分配**唯一**的应用标识、端口与数据库名。新增应用须先在本表登记，再写代码或改 Compose。

**人类可读副本**：[`docs-project/应用端口与命名注册表.md`](../../docs-project/应用端口与命名注册表.md)

## 分配原则

| 维度 | 规则 |
|------|------|
| 应用标识 `app_key` | 小写英文，全局唯一（如 `main`、`novel`、`agent`） |
| API 端口 | 7001 起递增，**禁止**两个服务共用同一宿主机端口 |
| 前端 Docker 端口 | 8080 起递增（Nginx 容器映射） |
| 前端 Vite dev 端口 | 5173 起递增（宿主机热更新，与 Docker 端口分离） |
| 数据库名 | `{app_key}_db` 或语义名（如 `admin_platform` = 主库）；**一库一业务域** |
| 容器名 | `ams-{role}-{app_key}` 或 `ams-api-{app_key}` |
| 环境变量 | 使用 `{APP_KEY}_` 前缀或 `VITE_*` / `POSTGRES_DB` 按应用隔离 |

## 已登记应用

| app_key | 说明 | 代码目录 | Compose service | 容器名 | API 端口 | 前端 Docker | Vite dev | 数据库名 |
|---------|------|----------|-----------------|--------|----------|-------------|----------|----------|
| `main` | 主应用（菜单 + Qiankun 基座） | `menu-master/` 或 `apps/main-*` | `api-main`, `main-frontend` | `ams-api-main`, `ams-main-frontend` | **7001** | **8080** | **5173** | `admin_platform` |
| `novel` | 小说子应用 | `apps/novel-*` | `api-novel`, `novel-frontend` | `ams-api-novel`, `ams-novel-frontend` | **7002** | **8081** | **5174** | `novel_db` |
| `agent` | Pi Agent（BFF + Pi） | `apps/agent-server/` | `agent-server` | `ams-agent-server` | **7003** | — | **7003** | `admin_platform`（元数据） |

## 共享基础设施

| 组件 | 容器名 | 端口 | 说明 |
|------|--------|------|------|
| PostgreSQL | `ams-postgres` | **5432** | 单实例，多 database |
| Redis | `ams-redis` | **6379** | 菜单缓存等；可按 app 分 DB index |

## 环境变量约定

**团队默认**：`deploy/config/.env.local`

```env
# 主应用 BFF
MAIN_PORT=7001
MAIN_POSTGRES_DB=admin_platform

# 小说子应用
NOVEL_PORT=7002
NOVEL_POSTGRES_DB=novel_db

# Agent
AGENT_PORT=7003

# 共享
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123
JWT_SECRET=CHANGE_ME_LOCAL_JWT
```

**各应用 `.env` / `.env.example`**（示例：主应用前端 `menu-master/frontend/.env`）：

```env
VITE_APP_KEY=main
VITE_API_BASE=http://localhost:7001/api
VITE_DEV_PORT=5173
# 子应用 entry（开发）按注册表逐个配置，如小说：
VITE_SUBAPP_NOVEL_ENTRY=http://localhost:5174
```

**禁止**：在源码中硬编码 `localhost:7001` 等；应读 `import.meta.env.VITE_*` 或 Egg `process.env.{APP}_PORT`。

## 新增子应用 checklist

1. 在本文档与 `docs-project/应用端口与命名注册表.md` 登记新行
2. 分配 API 端口（下一个可用：7004）、Vite dev（5175+）、Docker 前端（8082+）、`{app_key}_db`
3. 更新 `deploy/docker-compose.yml`、`deploy/config/.env.local`
4. 主应用菜单表增加 `microapp_name` / `route_prefix` 与 Qiankun `entry` 一致
5. 更新 `deploy/README.md` 与根 `README.md` 端口表

## Nginx / Qiankun entry 对照

| app_key | 生产 entry（经主应用 Nginx） | activeRule 示例 |
|---------|------------------------------|-----------------|
| `novel` | `/subapps/novel-app/` → `ams-novel-frontend:80` | `/media/novel` |
| （新子应用） | `/subapps/{microapp_name}/` | `/media/{route_prefix}` |

开发时 Qiankun `entry` 使用注册表中的 **Vite dev 端口**（如 `//localhost:5174`），勿与 API 端口混淆。
