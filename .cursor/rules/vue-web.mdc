---
description: Vue 3 Web SPA 前端开发规范（主应用与子应用）
globs: "{menu-master/**,project-sub/**,**/frontend/**,**/*.vue}"
alwaysApply: false
---

# Vue 3 Web 前端开发规范

## 架构

- 主应用与子应用均为 **Web SPA**，Vite 构建。
- 前后端通信仅通过 **HTTP RESTful API**（dev：Vite proxy；prod：Nginx 反向代理）。
- 子应用通过 Qiankun 以 HTTP entry 加载。

## 目录结构

```
{app}/frontend/src/
  components/
  views/          # 子应用
  composables/    # 主应用可选
  router/
  services/       # API 封装；子应用须含 apiConfig.js
  qiankun/        # 生命周期与配置
```

## 编码约定

- 组件 PascalCase（`.vue`）；composable / 工具 camelCase。
- API 调用封装在 `services/`，不在组件内直接 fetch。
- 环境变量：`VITE_APP_KEY`、`VITE_API_BASE`、`VITE_DEV_PORT`；主应用另用 `VITE_SUBAPP_*_ENTRY`。
- **禁止硬编码端口**；读 env 或 `app-registry.mdc`。
- UI：**Element Plus**；中文 locale 在 `main.js` 配置。
- **导航布局**：主应用与子应用独立 dev 均须垂直可折叠侧栏，见 **`nav-layout.mdc`**（子应用同样强制）。

## 子应用 apiConfig

Qiankun 嵌入时 **不得**用相对 `/api` 访问本子应用 BFF（会打到主应用 origin）。须检测 `__POWERED_BY_QIANKUN__` 并使用 **绝对 BFF URL**。详见 `subapp-onboarding.mdc`。

## 构建

- Vite + `@vitejs/plugin-vue`；子应用加 `vite-plugin-qiankun`（`useDevMode: true`）。
- 生产：多阶段 Docker 构建，构建期注入 `VITE_*`。
- **开发热更新**：见 **`vite-hmr.mdc`**（HMR、`hmr.clientPort`、Docker 源码挂载，强制）。

## 草稿与本地状态

- 多步骤表单草稿用 `sessionStorage` 或后端草稿 API。
