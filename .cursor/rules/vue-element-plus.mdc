---
description: Vue 3 + Element Plus 子应用 UI 开发规范
globs: "{**/views/**,**/components/**,**/*.vue}"
alwaysApply: false
---

# Vue 3 + Element Plus 规范

## 适用范围

微前端子应用 UI 层（如小说管理子应用），基于 Vue 3 + Element Plus + Vite。

## 组件与页面

- 页面放 `views/`，可复用组件放 `components/`。
- 优先使用 Element Plus 组件（`el-table`、`el-form`、`el-steps`、`el-container`），保持 UI 一致。
- 表单用 `el-form` + `v-model`；列表用 `el-table` + 分页/筛选。

## 状态与服务

- 全局状态用 **Pinia**（`stores/`）；API 调用封装在 `services/`，不在组件内直接 fetch。
- 多步骤表单（如新建小说）用 `el-steps` 分步，步骤间数据暂存到 store 或 `sessionStorage`。

## 路由

- 使用 **Vue Router**，`createWebHistory` 的 `base` 与 Qiankun 主应用对齐。
- 路由配置集中 `router/index.js`。

## 构建

- Vite + `@vitejs/plugin-vue` + `vite-plugin-qiankun`，`useDevMode: true` 用于开发。
- 生产构建分离 vendor chunk；**Vite dev 端口**按 `app-registry.mdc`（小说 **5174**），与主应用 Qiankun `entry` 一致。

## 图表

- 数据可视化用 **ECharts**（`vue-echarts`），与 Element Plus 主题协调。
