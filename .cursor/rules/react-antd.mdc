---
description: React + Ant Design 子应用 UI 开发规范
globs: "{**/renderer/**,**/views/**,**/components/**,**/*.tsx,**/*.jsx}"
alwaysApply: false
---

# React + Ant Design 规范

## 适用范围

微前端子应用 UI 层（如小说管理子应用），基于 React + Ant Design + Vite。

## 组件与页面

- 页面放 `views/`，可复用组件放 `components/`。
- 优先使用 Ant Design 组件（Table、Form、Steps、Layout），保持 UI 一致。
- 表单用 `Form` + 受控组件；列表用 `Table` + 分页/筛选。

## 状态与服务

- 全局状态放 `stores/`；API 调用封装在 `services/`，不在组件内直接 fetch。
- 多步骤表单（如新建小说）用 `Steps` 分步，步骤间数据暂存到 store 或 IPC/electron-store。

## 路由

- 使用 React Router，`basename` 与 Qiankun 主应用对齐。
- 路由配置集中 `router/index.tsx`。

## 构建

- Vite + `vite-plugin-qiankun`，`useDevMode: true` 用于开发。
- 生产构建分离 vendor chunk；dev 端口与主应用注册 entry 一致。

## 图表

- 数据可视化用 Ant Design Charts，与 Ant Design 主题保持一致。
