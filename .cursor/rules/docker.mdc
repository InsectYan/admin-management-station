---
description: Docker 容器化部署规范（deploy/ + ams CLI）
globs: "{deploy/**,docker/**,**/Dockerfile*,**/docker-compose*.yml,.env*}"
alwaysApply: false
---

# Docker 部署规范

## 原则

- **分层编排**：共享 `deploy/compose/infra.yml`；各应用在自身目录提供 `docker-compose.yml` + `docker-compose.services.yml`；根 `deploy/docker-compose.yml` **include** 全部应用片段。详见 **`docker-compose.mdc`**。
- 日常启动用 **`ams local`**（默认主应用栈），全栈用 **`ams local:all`**。
- 配置分层：`deploy/config/.env.local`（团队）+ 各应用 `.env`（个人密钥）。
- **端口与数据库名**：以 **`app-registry.mdc`** 为准；新增服务先登记再改 Compose。

## 目录

```
deploy/
  compose/infra.yml          # 共享 DB + Redis + ams-net
  docker-compose.yml         # 根 include 入口
  config/.env.local
menu-master/
  docker-compose.yml         # 主应用独立栈
  docker-compose.services.yml
  docker/*.Dockerfile
apps/{app_key}/
  docker-compose.yml
  docker-compose.services.yml
```

## 镜像

- 前端：多阶段 Node 构建 + Nginx。
- Egg.js BFF：Node Alpine，`npm ci --omit=dev`。
- Agent：`deploy/config/Dockerfile.agent`（BFF + Pi）。
- 数据库：`postgres:16-alpine` + 数据卷。

## 网络

- 容器互访用 **service 名**（如 `postgres`、`agent-server`）。
- 主应用 Nginx：`/api/` → api-main，`/agent-api/` → agent-server，`/subapps/` → 子应用。

## CLI

见 `.cursor/rules/deploy-cli.mdc`。新增服务时同步更新 `deploy/docker-compose.yml` 与 `ams help`。

详细拓扑：`docs-project/部署与Docker方案.md`
