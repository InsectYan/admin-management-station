---
description: Windows 控制台与 PowerShell 中文输入输出规范（deploy/scripts、ams CLI）
globs: "{deploy/scripts/**/*.ps1,deploy/scripts/**/*.mjs}"
alwaysApply: false
---

# Windows 控制台中文 I/O 规范

## 问题背景

中文 Windows 默认代码页常为 **GBK (CP936)**。若 `deploy/scripts/*.ps1` 以 **UTF-8 无 BOM** 保存：

1. **Windows PowerShell 5.1** 按系统 ANSI 解析脚本，中文字符串引号错位；
2. 表现为乱码（如 `鏃犳硶杩炴帴`），且 `Write-Host`、`-ForegroundColor` 等被当作普通文本输出；
3. 根因是**脚本解析失败**，不仅是终端显示编码问题。

PowerShell 7+ (`pwsh`) 默认 UTF-8，但仍建议统一以下约定，保证 PS 5.1 与 Cursor 终端一致。

## 强制约定

| 项 | 要求 |
|----|------|
| `.ps1` 编码 | **UTF-8 with BOM**（见 `deploy/scripts/.editorconfig`） |
| 脚本入口 | 有 `param` 时 **`param` 块须在最前**（仅注释可在其上）；随后 dot-source `_utf8.ps1` |
| 无 `param` 脚本 | 注释后首行 dot-source `_utf8.ps1` |
| `_utf8.ps1` | 仅 ASCII 源码；负责 `chcp 65001` 与 `[Console]::OutputEncoding` |
| 用户可见中文 | 允许；须满足上述编码与 `_utf8` 初始化 |
| Node 调度 | `run.mjs` 调用 PowerShell 时优先 `pwsh`，回退 `powershell.exe` |

## 新增 / 修改 PowerShell 脚本 checklist

1. 文件保存为 **UTF-8 BOM**（可用 `[System.IO.File]::WriteAllText(..., UTF8Encoding($true))` 或遵守 `.editorconfig`）
2. 有 `param` 时保持 `param` 在首行；其后 dot-source `_utf8.ps1`
3. 中文提示用 `Write-Host "..." -ForegroundColor ...`，勿在字符串外拼接参数
4. 在中文 Windows 终端验证：故意触发一条中文错误提示，确认无乱码、无裸 `Write-Host` 输出

## Node (`run.mjs`) 约定

- `spawnSync` 继承 `stdio: 'inherit'`，不额外指定错误编码
- Windows 下设置 `CHCP=65001` 环境变量（辅助子进程）
- 优先检测并使用 **PowerShell 7**（`pwsh`），路径可通过 `AMS_PWSH` 覆盖

## 禁止

- UTF-8 **无 BOM** 保存含中文的 `.ps1`
- 在多个 ps1 中重复复制 UTF-8 初始化逻辑（统一用 `_utf8.ps1`）
- 仅改终端 `chcp` 而不修复脚本文件 BOM（无法解决解析乱码）

## 相关

- `deploy-cli.mdc` — `ams` 命令与脚本目录
- `deploy/scripts/.editorconfig`
