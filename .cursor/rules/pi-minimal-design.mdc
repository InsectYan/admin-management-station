---
description: Pi 极简原则（子应用内 agent 模块）
globs: ["novel-sub/agent/**/*", "novel-sub/workspace-templates/**/*"]
alwaysApply: false
---

# Pi 极简设计

工作区位于 **`{app}/workspace-templates/`**（如 `novel-sub/`），不在仓库根。

## 允许 / 禁止

| 允许 | 禁止 |
|------|------|
| BFF 拼 context、校验 outbox | TS 关键词硬编码 agent 路由 |
| Pi 读 inbox + format 决策 | LangGraph / Python 子进程 |
| tools/*.mjs 调 internal API | Pi 直接写业务库 |

## 构建

```bash
cd novel-sub/agent && npm run build
```

Docker 构建见 `novel-sub/deploy/config/Dockerfile.agent`。
