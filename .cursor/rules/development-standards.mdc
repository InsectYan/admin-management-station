---
description: 项目核心开发规范，适用于所有代码与文档变更
alwaysApply: true
---

# 开发规范

本文件定义 admin-management-station 工作区的通用开发约束。**编写、修改或审查代码时均须遵循。**

## 自包含应用（最高优先级）

**每个主应用、子应用必须是可独立拆出的完整项目**，自带 `frontend`、`backend`、`database`、`deploy`；PostgreSQL 由各应用 compose 管理，缓存默认 **memory**（见 `cache-local.mdc`），**禁止**仓库级共享 infra。

详见 **[`app-self-contained.mdc`](./app-self-contained.mdc)**。与本节冲突的旧约定（如 `apps/`、`deploy/compose/infra.yml`）一律以自包含规则为准。

## 子应用强制遵循

**`project-sub/` 下所有子应用与主应用适用同一套 `.cursor/rules/` 规范**，不得以「子应用特例」跳过或降级。新建或修改子应用时须至少满足：

- 架构：`app-self-contained.mdc`、`app-registry.mdc`、`cache-local.mdc`
- 前端：`vue-web.mdc`、`vue-element-plus.mdc`、**`nav-layout.mdc`**、**`subapp-layout.mdc`**、**`subapp-style-isolation.mdc`**、**`vite-hmr.mdc`**
- 微前端：`subapp-onboarding.mdc`、`qiankun-microfrontend.mdc`
- 后端：`egg-backend.mdc`、`postgresql.mdc`、**`database-schema-sync.mdc`**（涉及 BFF / 数据库时）
- 部署：`deploy-cli.mdc`、`docker.mdc`、`docker-compose.mdc`（涉及 `deploy/` 时）

审查子应用 PR 或代码时，按上述清单逐项核对；不符合须修正后再合并。

## 基本原则

- **最小改动**：只修改与当前任务直接相关的内容。
- **遵循现有约定**：匹配项目已有的命名、目录结构与代码风格。
- **可读优先**：代码自解释；仅在非直观处添加必要注释。
- **可维护性**：避免过度抽象；优先复用已有模块。

## 代码质量

- 变更可正常运行，不引入明显错误或 lint 问题。
- 不提交敏感信息（密钥、`.env` 等）。
- 错误处理明确、可追踪。
- 仅在任务明确要求时添加测试。

## Git 与协作

- 未经明确要求，不主动 commit 或 push。
- 不执行破坏性 git 操作，除非明确要求。

## 技术栈规范索引

| 规则文件 | 适用范围 |
|---------|---------|
| **`app-self-contained.mdc`** | 自包含应用目录结构 |
| **`app-registry.mdc`** | 端口、库名、容器名 |
| **`cache-local.mdc`** | 进程内缓存（默认 memory） |
| **`subapp-onboarding.mdc`** | 子应用接入 Qiankun |
| **`nav-layout.mdc`** | 垂直导航菜单与折叠 |
| **`subapp-layout.mdc`** | 子应用视口高度与内容滚动 |
| **`subapp-style-isolation.mdc`** | 子应用 CSS 隔离 |
| **`vite-hmr.mdc`** | Vite 开发热更新（HMR） |
| `deploy-cli.mdc` | `ams-{app_key}` CLI |
| `docker.mdc` / `docker-compose.mdc` | 容器与 compose（概要 / 目录结构） |
| `vue-web.mdc` | Vue 3 前端 |
| `vue-element-plus.mdc` | Element Plus 子应用 UI |
| `egg-backend.mdc` | Egg.js BFF |
| `postgresql.mdc` | PostgreSQL / ORM |
| **`database-schema-sync.mdc`** | 启动时 DB Schema 自动同步 |
| `qiankun-microfrontend.mdc` | 微前端集成 |
| `windows-console.mdc` | Windows PowerShell UTF-8 |

## 开发流程 Skills

| Skill | 路径 | 用途 |
|-------|------|------|
| 主应用 | `skills/main-app-developer/` | 基座 + 菜单 + Qiankun 注册 |
| 子应用（通用） | `skills/sub-app-developer/` | 自包含子应用脚手架与接入 |
| 按项目扩展 | `skills/project-developer/{app}/` | 在已有子应用上按设计文档迭代 |

规范冲突时，以 `.cursor/rules/` 为准。
