---
description: 新子应用接入主应用（Qiankun）检查清单与常见陷阱
globs: "{menu-master/**,project-sub/**,**/deploy/**,**/qiankun/**,**/frontend/**,**/backend/**}"
alwaysApply: false
---

# 子应用接入主应用 — 规范与避坑

新增子应用时**须先读本文件**。参考实现见 `project-sub/novel-sub/` 及 `subapp.manifest.json`。

## 1. 登记与 manifest（主库）

| 步骤 | 位置 | 要求 |
|------|------|------|
| 登记端口 | `app-registry.mdc` | API / Vite / PG 宿主机端口 |
| 子应用 manifest | `project-sub/{app}/subapp.manifest.json` | 端口、microapp_name、route_prefix 等 |
| 自动同步 | `ams-main local` 或 `ams-main sync:subapps` | 写入 `subapp_registry` + 一级 `menu_items` |
| 增量迁移 | `migrations/` | 仅当需手工 SQL 变更时使用 |

菜单 API 须返回 `entry`、`active_rule`、`basename`。

环境变量覆盖 entry：`SUBAPP_{KEY}_ENTRY`。

## 2. 主应用 JWT 白名单（必做）

`egg-jwt` 全局启用时，**公开读接口**（如 `GET /api/menus`）须加入 `ignore`，否则侧栏 401。

```javascript
config.jwt = {
  enable: true,
  ignore: ctx => {
    if (ctx.path === '/api/health') return true;
    if (ctx.method === 'GET' && (ctx.path === '/api/menus' || ctx.path === '/api/menus/root')) return true;
    return false;
  },
};
```

## 3. Qiankun entry（浏览器拉取）

| 误区 | 正确 |
|------|------|
| entry 用 `host.docker.internal` | 浏览器须用 `http://localhost:{Vite端口}` |
| compose 映射端口与 registry 不一致 | 宿主机端口 = `app-registry` Vite 端口 |
| 只启动主应用就点子菜单 | 子应用 frontend **须已启动** |

配置：主应用 `VITE_SUBAPP_{KEY}_ENTRY`、Docker `SUBAPP_{KEY}_ENTRY`、主库 `subapp_registry.entry_dev`。

## 4. 子应用 API 基址

Qiankun 嵌入后 origin 为主应用；相对 `/api` 会打到主 BFF → JWT 401。

```javascript
if (qiankunWindow.__POWERED_BY_QIANKUN__) {
  return `http://localhost:${import.meta.env.VITE_API_PORT}/api`; // 或 env 绝对 URL
}
return import.meta.env.VITE_API_BASE || '/api';
```

| 场景 | 子 BFF 基址 |
|------|------------|
| 独立 dev | `/api` + Vite proxy |
| Qiankun 嵌入 | `http://localhost:{API端口}/api` |

子 BFF 须启用 `egg-cors`。

## 5. 子应用 Egg.js

```javascript
exports.sequelize = { enable: true, package: 'egg-sequelize' };
exports.cors = { enable: true, package: 'egg-cors' };
```

`app.js` 须在 `beforeStart` 调用 `syncSchemaOnStartup`（见 **`database-schema-sync.mdc`**）。

| 场景 | `POSTGRES_PORT` |
|------|-----------------|
| 容器内 | **5432** |
| 宿主机 dev | **宿主机映射端口**（见 `backend/.env`） |

## 6. Docker 清单

- `package-lock.json` 存在于 backend、frontend
- Dockerfile：`npx egg-scripts start --workers=1`，禁止 `--daemon`
- compose 使用 `compose.ps1` + `--env-file config/.env.local`
- Vite 端口映射与 registry 一致
- 前端 service **bind-mount 源码** + Vite HMR 配置（见 **`vite-hmr.mdc`**）

## 6.1 开发热更新（HMR）

新增或修改子应用 `vite.config.js` / `docker-compose.yml` 时须满足 **`vite-hmr.mdc`**：

- `server.hmr.clientPort` = 本子应用 Vite 端口
- `watch.usePolling: true`
- compose 挂载 `frontend/src`、`index.html`、`vite.config.js`

## 7. 嵌入态 UI

- `__POWERED_BY_QIANKUN__`：隐藏子应用品牌与垂直侧栏，保留页内水平 `NavMenu`（见 **`nav-layout.mdc`**）
- **独立 dev**：子应用须实现垂直可折叠侧栏，不得仅用水平顶栏
- 内容区与主应用 `#subapp-container` 样式协调；子应用须满足 **`subapp-layout.mdc`**（菜单吸顶、内容区内滚动）

## 8. 联调顺序

```text
1. ams-{sub} local
2. 确认 localhost:{vite} 与 localhost:{api}/api/health
3. ams-main local          # 含 sync-subapps 自动注册菜单
4. 主应用浏览器 → 点击菜单
```

## 9. Checklist

- [ ] `app-registry.mdc` 登记
- [ ] `subapp.manifest.json` 已编写
- [ ] `ams-main sync:subapps` 或 `ams-main local` 已同步
- [ ] 主应用 JWT ignore 菜单 GET
- [ ] 子应用 `apiConfig.js` + entry env
- [ ] compose 端口与 registry 一致
- [ ] compose 内 `POSTGRES_PORT=5432`
- [ ] `egg-sequelize`、`egg-cors` 已启用
- [ ] `app.js` 已注册启动 schema 同步（`database-schema-sync.mdc`）
- [ ] 样式隔离（`subapp-style-isolation.mdc`）
- [ ] `NavMenu` + `useNavCollapse` 垂直侧栏（独立 dev）；嵌入态水平 NavMenu
- [ ] `vite.config.js` HMR + compose 前端源码挂载（见 **`vite-hmr.mdc`**）
- [ ] README 联调步骤

## 禁止

- Qiankun 下相对 `/api` 调子 BFF
- entry 仅容器内地址
- Docker 内 ORM 连 PG 宿主机映射端口
- 无 lockfile 的 `npm ci`
