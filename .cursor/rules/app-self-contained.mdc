---
description: 自包含应用架构——每个主应用/子应用均为可独立拆出的完整项目
globs: "{menu-master/**,novel-sub/**,**/deploy/**,docs-project/**}"
alwaysApply: true
---

# 自包含应用架构（强制）

本 monorepo 是**工作区聚合**；**每个主应用、子应用本身必须是完整项目**，脱离本仓库仍可独立开发、部署与运行。

## 1. 每个应用须自带

| 模块 | 路径 | 说明 |
|------|------|------|
| 前端 | `{app}/frontend/` | Vue 3 + Vite；子应用含 Qiankun 生命周期 |
| 服务端 BFF | `{app}/backend/` | Egg.js REST API |
| 数据库 | `{app}/database/` | `init.sql`；ORM 模型在 `backend/app/model/` |
| 缓存 | `{app}/backend/app/lib/cache.js` | 默认 **memory**（见 `cache-local.mdc`） |
| 部署 | `{app}/deploy/` | compose + `ams-{app_key}` CLI + Dockerfile |

**禁止**出现「只有 frontend / 只有 deploy 壳」的仓库级目录。

## 2. 基础设施归属

- **PostgreSQL 由各应用自己的 `deploy/docker-compose.yml` 管理**，不得依赖仓库根级共享 infra。
- **缓存默认进程内 memory**，不依赖 Redis 容器（见 `cache-local.mdc`）。
- 并行开发时通过 **不同宿主机端口** 区分 Postgres（见 `app-registry.mdc`）；容器内仍用 `postgres:5432`。
- 容器互访用 **Compose service 名**，禁止 `localhost`。

## 3. 可独立拆出检验

复制 `{app}/` 整目录后须能：

1. `cd deploy && npm link && ams-{app_key} local` 拉起 DB + API + 前端
2. 或分别 `backend` / `frontend` 宿主机 `npm run dev`（文档写清端口与 `.env`）
3. 不依赖 monorepo 根目录、`apps/`、根 `deploy/compose/infra.yml`

## 4. 新增应用

1. 复制已有 `{app}/` 骨架
2. 在 `app-registry.mdc` 与 `docs-project/应用端口与命名注册表.md` 登记端口
3. 按 `subapp-onboarding.mdc` 完成接入（子应用）
4. 实现完整 `{app}/deploy/docker-compose.yml`
5. 更新 `{app}/README.md`

## 5. 禁止

- 仓库根 `deploy/compose/infra.yml` 或跨应用 include 共享 Postgres/Redis
- `apps/` 等半套服务目录
- 文档/规则中出现「全仓共用一个 postgres」作为默认方案
