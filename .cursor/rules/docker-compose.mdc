---
description: Docker Compose 与 deploy 目录规范（每应用自包含栈）
globs: "{menu-master/**,novel-sub/**,**/deploy/**,**/docker-compose*.yml,**/Dockerfile*}"
alwaysApply: false
---

# Docker Compose 与 deploy 规范

## 架构原则

| 层级 | 路径 | 职责 |
|------|------|------|
| 应用 deploy | `{app}/deploy/` | Postgres + API + 前端 |
| 镜像 | `{app}/deploy/config/Dockerfile.*` | canonical Dockerfile |

**目标**：`ams-{app_key} local` 仅依赖 `{app}/deploy/docker-compose.yml`，**不 include 仓库根 infra**。

## deploy 目录

```
{app}/deploy/
├── docker-compose.yml
├── package.json + bin/ams-{app_key}
├── config/
│   ├── .env.local
│   ├── Dockerfile.backend
│   └── Dockerfile.frontend
└── scripts/
    ├── run.mjs
    └── compose.ps1 / compose.sh
```

## compose 要点

- `postgres` 写在同一文件；`CACHE_DRIVER=memory`（见 `cache-local.mdc`）
- 容器内 `POSTGRES_PORT=5432`；宿主机映射端口见 `app-registry.mdc`
- 前端 `ports:` 与 registry Vite 端口一致
- `backend`、`frontend` 提交 `package-lock.json`
- init SQL：`../database/init.sql`
- 独立 network / volume 名带 app 前缀
- **禁止** `include: ../../deploy/compose/infra.yml`

## 禁止

- 根 `deploy/compose/infra.yml` 作为默认依赖
- `apps/` 目录编排
- 应用根目录冗余 `docker-compose.yml`
- 容器互访使用 `localhost`

## 相关规则

`app-self-contained.mdc` · `docker.mdc` · `deploy-cli.mdc` · `app-registry.mdc` · `subapp-onboarding.mdc`
