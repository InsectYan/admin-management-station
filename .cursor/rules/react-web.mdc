---
description: React Web SPA 前端开发规范（主应用与子应用）
globs: "{apps/**,frontend/**,**/renderer/**,**/*.tsx,**/*.jsx}"
alwaysApply: false
---

# React Web 前端开发规范

## 架构

- 主应用与子应用均为 **Web SPA**，Vite 构建，Nginx 容器托管静态资源。
- 前后端通信仅通过 **HTTP RESTful API**（开发时 Vite proxy，生产时 Nginx 反向代理）。
- 子应用通过 Qiankun 以 HTTP entry 加载，独立 Dockerfile 部署。

## 目录结构

**主应用**：

```
apps/main-frontend/src/
  components/
  containers/
  hooks/
  services/
  common/enums/
```

**子应用**：

```
apps/novel-frontend/src/
  components/
  views/
  stores/
  services/
  router/
  qiankun/
```

## 编码约定

- 组件 PascalCase；工具/钩子 camelCase。
- 枚举放 `common/enums`，全大写下划线，ES Module 导出。
- API 调用封装在 `services/`，组件内不直接裸 fetch。
- 环境变量：`VITE_API_BASE` 等，不硬编码密钥。

## 草稿与本地状态

- 多步骤表单草稿用 `sessionStorage` 或后端草稿 API。

## Docker

- 前端镜像多阶段构建：Node 构建 + Nginx 运行。
- 构建参数注入 `VITE_*` 环境变量。
