---
description: Node.js Egg.js 后端 BFF 开发规范
globs: "{**/backend/**,**/app/controller/**,**/app/service/**,**/app/model/**,**/config/**}"
alwaysApply: false
---

# Egg.js 后端开发规范

## 分层职责

- **Controller**：参数校验、调用 Service、组装 HTTP 响应。
- **Service**：业务逻辑与事务编排。
- **Model**：ORM 映射（`app/model/`）。
- 路由：`app/router.js`，API 前缀 `/api`。

## 目录约定

```
{app}/backend/
  app/
    controller/
    service/
    model/
    middleware/
  config/
    config.default.js
    plugin.js
  app.js
```

## 响应格式

```javascript
{ code: 200, message: 'success', data: /* payload */ }
```

错误：`code` 为 HTTP 语义码，`message` 可读，`data` 可为 `null`。

## 插件与配置

- 数据库：`egg-sequelize` + PostgreSQL（`plugin.js` 须 `enable: true, package: 'egg-sequelize'`）。
- 子应用 BFF：须 `egg-cors`（Qiankun 跨 origin）。
- 认证：`egg-jwt`；主应用对公开读接口配置 `jwt.ignore`（见 `subapp-onboarding.mdc`）。
- 写操作：JWT + RBAC / 权限中间件。
- 时区：`Asia/Shanghai`（`app.js`）。
- 配置：`config.default.js` + `config.local.js`；敏感项走环境变量。
- **Schema 同步**：`app.js` `beforeStart` 须调用 `syncSchemaOnStartup`（见 `database-schema-sync.mdc`）。
- **多应用**：独立 `PORT` 与 `POSTGRES_DB`（见 `app-registry.mdc`）。
- **Docker 内 DB**：`POSTGRES_HOST=postgres`、`POSTGRES_PORT=5432`；宿主机 dev 用映射端口。

## Docker 内启动

```dockerfile
CMD ["npx", "egg-scripts", "start", "--title={app}-backend", "--workers=1"]
```

禁止 `npm run start`（`--daemon` 会导致容器退出）。

## 安全

- 禁止 SQL 字符串拼接；使用 ORM 或参数化查询。
- 写接口（POST/PUT/DELETE）须鉴权。
- 日志用 `ctx.app.logger`，不输出密码/令牌。
