---
description: Node.js Egg.js 后端 BFF 开发规范
globs: "{electron/**,back-end/**,**/app/controller/**,**/app/service/**,**/app/model/**,**/config/**}"
alwaysApply: false
---

# Egg.js 后端开发规范

## 分层职责

- **Controller**：参数校验、调用 Service、组装 HTTP 响应；不含业务逻辑。
- **Service**：业务逻辑与事务编排。
- **Model**：数据模型与 ORM 映射（`app/model/`）。
- 路由定义在 `app/router.js` 或 `app/route/`，API 前缀 `/api`。

## 目录约定

```
electron/  或  back-end/
  app/
    controller/
    service/
    model/
    middleware/
  config/
    config.default.js
    plugin.js
  app.js
```

## 响应格式

统一 JSON 结构：

```javascript
{ code: 200, message: 'success', data: /* payload */ }
```

错误时：`code` 为 HTTP 语义码（400/403/500），`message` 可读，`data` 可为 `null`。

## 插件与配置

- 数据库：`egg-orm` 或 Sequelize 连接 PostgreSQL。
- 认证：`egg-jwt`；写操作需 JWT + RBAC 中间件。
- 时区：`Asia/Shanghai`（在 `app.js` 中设置）。
- 配置分环境：`config.default.js` + `config.local.js`；敏感项走环境变量。

## 安全

- 禁止 SQL 字符串拼接；使用 ORM 或参数化查询。
- 写接口（POST/PUT/DELETE）必须鉴权与权限校验。
- 日志用 `ctx.app.logger`，不输出密码/令牌。
