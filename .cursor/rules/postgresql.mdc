---
description: PostgreSQL 数据库设计与访问规范
globs: "{**/*.sql,**/migrations/**,**/app/model/**,**/database/**}"
alwaysApply: false
---

# PostgreSQL 开发规范

## 命名

- 表名：小写复数 + 下划线（如 `menu_items`、`novels`）。
- 字段：小写 + 下划线。
- 主键：`id`，`SERIAL` 或 `BIGSERIAL`。
- 外键：`{关联表单数}_id`（如 `user_id`、`novel_id`）。
- 时间戳：每张业务表含 `created_at`、`updated_at`，默认 `CURRENT_TIMESTAMP`。

## 索引

- 外键、频繁筛选字段建 B-tree 索引。
- JSONB 查询字段建 GIN 索引（如 `plot`、`profile`）。
- 复合查询建组合索引（如 `(novel_id, chapter_number)`）。

## 数据类型

- 半结构化数据用 `JSONB`，不用 TEXT 存 JSON。
- 状态/角色等有限枚举用 `VARCHAR` + 应用层枚举约束。
- 长文本（章节内容）用 `TEXT`。

## 访问层

- 通过 ORM（Sequelize / TypeORM）定义模型，避免裸 SQL。
- 多步写操作用事务；读多写少场景考虑缓存（Redis）。
- 生产环境：`synchronize: false`，变更走 migration。
- 禁止在应用层拼接用户输入到 SQL。

## 设计文档对照

设计文档中每张表须包含：`created_at`、`updated_at`、必要索引与外键约束；缺失时在实现前反馈设计缺陷。
