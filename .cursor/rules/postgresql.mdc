---
description: PostgreSQL 数据库设计与访问规范
globs: "{**/*.sql,**/migrations/**,**/app/model/**,**/database/**}"
alwaysApply: false
---

# PostgreSQL 开发规范

## 命名

- 表名：小写复数 + 下划线（如 `users`、`order_items`）。
- 字段：小写 + 下划线。
- 主键：`id`，`SERIAL` 或 `BIGSERIAL`。
- 外键：`{关联表单数}_id`。
- 时间戳：每张表含 `created_at`、`updated_at`，默认 `CURRENT_TIMESTAMP`。

## 索引

- 外键、频繁筛选字段建 B-tree 索引。
- JSONB 查询字段建 GIN 索引。
- 复合查询建组合索引。

## 数据类型

- 半结构化数据用 `JSONB`。
- 有限枚举用 `VARCHAR` + 应用层约束。
- 长文本用 `TEXT`。

## 访问层

- 通过 ORM（Sequelize）定义模型于 `app/model/`，避免裸 SQL。
- 多步写操作用事务；读多写少用 `app/lib/cache.js`（见 `cache-local.mdc`）。
- 生产：`synchronize: false`，变更走 migration。
- 禁止拼接用户输入到 SQL。

## 多应用隔离

- 每个 `app_key` 使用独立 `POSTGRES_DB`（见 `app-registry.mdc`）。
- 禁止多个 BFF 混连同一业务库（主库 `subapp_registry` 除外，仅主应用 BFF 访问）。

## 与设计文档协作

实现前对照 `docs-project/` 中对应设计文档的 DDL；缺失 `created_at`/`updated_at`/索引/外键时，在编码前反馈设计缺陷（见 `skills/project-developer/{app}/`）。
