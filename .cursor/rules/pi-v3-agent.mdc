---
description: Pi v3 Agent 开发规范（位于子应用 {app}/agent/ 内）
globs: ["novel-sub/agent/**/*", "novel-sub/workspace-templates/**/*", "novel-sub/workspaces/**/*"]
alwaysApply: false
---

# Pi v3 Agent — 子应用内智能体

> 对齐 [cartoon-agent](E:/AI_Projects/cartoon-agent) · 定稿见 `docs-project/Agent开发方案.md`  
> **架构**：Agent **不是**仓库级独立服务，而是 **`{app}/agent/`** 模块。

## 拓扑（小说子应用示例）

```
novel-sub/frontend  ──HTTP/SSE──►  novel-sub/agent/ (:7003)
        │                                    │
        └── REST ──► novel-sub/backend (:7002)
                          │
                          └── novel-sub/deploy/postgres (novel_db)
```

## 工作目录（同应用内）

| 路径 | 角色 |
|------|------|
| `novel-sub/workspace-templates/` | Pi 模板 |
| `novel-sub/workspaces/` | 运行时（gitignore） |
| `novel-sub/data/` | artifacts（gitignore） |

环境变量 `TEMPLATES_ROOT` 等默认指向**应用根**相对路径，不依赖 monorepo 根。

## 核心 API（agent 进程）

| 路径 | 说明 |
|------|------|
| `POST /api/chat` | 对话（SSE） |
| `GET /api/llm/profiles` | 模型列表 |
| `GET /api/internal/**` | Pi tools 读本应用数据 |

## 单轮管线

```
前端 → chat → enrichMessage → runTurn → outboxPersist → SSE
```

## 改代码索引

| 目标 | 位置 |
|------|------|
| SSE 路由 | `novel-sub/agent/src/bff/routes/chat.ts` |
| Pi 单轮 | `novel-sub/agent/src/agent/runTurn.ts` |
| 域模板 | `novel-sub/workspace-templates/novel/AGENTS.md` |

## 禁止

- 在 `apps/agent-server/` 或仓库根维护 Agent 代码
- Pi 直接写业务 PostgreSQL 表（经 BFF 契约）
- TypeScript 关键词硬编码路由 `agent_name`
