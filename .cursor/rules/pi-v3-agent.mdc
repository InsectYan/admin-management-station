---
description: Pi v3 Agent 开发规范（对齐 cartoon-agent）
globs: ["apps/agent-server/**/*", "apps/*/workspace-templates/**/*", "apps/*/workspaces/**/*"]
alwaysApply: false
---

# Pi v3 Agent — 平台智能体漫游

> 对齐参考：[cartoon-agent](E:/AI Tools/projects/cartoon-agent) · 定稿见 `docs-project/Agent开发方案.md`  
> **极简原则**：`.cursor/rules/pi-minimal-design.mdc`

## 拓扑（本平台）

```
主应用 / 子应用（React + Qiankun）
        │ HTTP / SSE
        ▼
apps/agent-server/（Node，`app_key=agent`，端口 **7003**）
  ├── BFF 业务 API（对话 enrichment、artifacts）
  ├── Pi AgentManager（按业务域工作目录）
  └── internal API（供 workspace tools 读平台数据）
        │
        └─► PostgreSQL（`admin_platform`，见 `app-registry.mdc`）
```

平台菜单/鉴权由 `menu-master/backend`（Egg.js）负责；**Agent 能力独立进程**。

## 工作目录（子应用域内，非仓库根）

Pi 的 `workspace-templates/`、`workspaces/`、`data/` **不在 monorepo 根目录**配置，而由**具体业务子应用/Agent 域**自带：

| 路径（示例） | 角色 |
|-------------|------|
| `apps/{domain}/workspace-templates/` | 模板（SOUL.md、AGENTS.md、tools/） |
| `apps/{domain}/workspaces/` | 运行时工作区（gitignore） |
| `apps/{domain}/data/` | artifacts JSON（gitignore，可选） |

实现 `agent-server` 时通过环境变量（如 `TEMPLATES_ROOT`、`WORKSPACES_ROOT`）指向上述目录，并在该子应用的 `deploy/docker-compose.yml` 中挂载卷。

## 逻辑子 Agent（agent_name）

**非独立进程**；Pi 单轮内通过 `agent_name` + `message_type` 区分（见各域 `AGENTS.md`）。

## 核心 API（agent-server）

| 路径 | 说明 |
|------|------|
| `POST /api/chat` | 对话（SSE） |
| `GET /api/llm/profiles` | 模型列表 |
| `GET /api/internal/**` | Pi tools 读平台数据 |

## 单轮管线

```
前端 → chat route → enrichMessage → runTurn → outboxPersist → SSE
```

- `inbox.md` = BFF enrichment（含 `## 当前消息`）
- `outbox.json` = Pi 写出；BFF 落库 / 写 artifacts
- 无 outbox → 通用补写 + fallback（见 cartoon-agent `creatorTurnFallback`）

## 改代码索引

| 目标 | 位置（实现后） |
|------|----------------|
| SSE 路由 | `apps/agent-server/src/bff/routes/` |
| enrichment | `apps/agent-server/src/bff/services/enrich*.ts` |
| Pi 单轮 | `apps/agent-server/src/agent/runTurn.ts` |
| 落库 | `apps/agent-server/src/bff/services/outboxPersist.ts` |
| 域模板 | 各子应用内 `workspace-templates/**/AGENTS.md` |

## 禁止

- 在仓库根目录新增 `workspace-templates/`、`workspaces/`、`data/`
- Python LangGraph / 独立 Python Agent 子进程
- TypeScript 关键词硬编码路由 `agent_name`
- Pi 层直接 write 业务库（须经 BFF 契约）
