# 小说子应用部署配置（占位）

| 文件 | 说明 |
|------|------|
| `.env.local` | 本地 Docker 默认 |
| `Dockerfile.backend` | BFF 占位，实现后迁至 `deploy/config/Dockerfile` |
| `Dockerfile.frontend` | 占位；实现后在 `novel-frontend/Dockerfile` 使用 Vite dev |
