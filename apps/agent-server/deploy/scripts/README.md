# CLI 命令（Agent）

| 命令 | 作用 |
|------|------|
| `ams-agent local` | 启动 infra + agent-server（profile agent） |
| `ams-agent local:infra` | 仅 DB + Redis |
| `ams-agent local:reset` | 清库重建 |
| `ams-agent local:down` | 停止本栈 |
