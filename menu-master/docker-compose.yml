# 主应用独立 Docker 栈（infra + API + 前端）
# 启动：cd menu-master && docker compose up -d --build
# 或：ams local:main

name: ams-main

include:
  - path: ../deploy/compose/infra.yml
    env_file: ../deploy/config/.env.local
  - path: ./docker-compose.services.yml
