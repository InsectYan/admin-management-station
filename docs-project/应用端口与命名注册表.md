# 应用端口与命名注册表

> **规范来源**：`.cursor/rules/app-registry.mdc` · **架构**：`.cursor/rules/app-self-contained.mdc`  
> 每个应用**自带 Postgres/Redis**，并行开发时使用不同**宿主机端口**。

## 已登记应用

| app_key | 应用根目录 | CLI | API | Agent | Vite | PG 宿主机 | Redis 宿主机 | 数据库 |
|---------|-----------|-----|-----|-------|------|-----------|--------------|--------|
| `main` | `menu-master/` | `ams-main` | 7001 | — | 5173 | **5432** | **6379** | `admin_platform` |
| `novel` | `novel-sub/` | `ams-novel` | 7002 | **7003** | 5174 | **5433** | **6380** | `novel_db` |

## 容器命名

| 应用 | Postgres | Redis | API | Agent | 前端 |
|------|----------|-------|-----|-------|------|
| main | `ams-main-postgres` | `ams-main-redis` | `ams-api-main` | — | `ams-main-frontend` |
| novel | `ams-novel-postgres` | `ams-novel-redis` | `ams-api-novel` | `ams-novel-agent` | `ams-novel-frontend` |

## 已废弃

- 全仓共享 `ams-postgres` / `deploy/compose/infra.yml`
- 独立 `app_key=agent` 与 `apps/agent-server/`
- `apps/` 目录

## 新增子应用

1. 复制 `novel-sub/` 或 `menu-master/` 整目录
2. 在本表登记 API/PG/Redis 宿主机端口（递增）
3. 实现完整 `{app}/deploy/docker-compose.yml` 并验证可独立 `ams-{app_key} local`
