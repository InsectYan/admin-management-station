# 应用端口与命名注册表



> **规范来源**：`.cursor/rules/app-registry.mdc` · **架构**：`.cursor/rules/app-self-contained.mdc` · **缓存**：`.cursor/rules/cache-local.mdc`  

> 每个应用**自带 Postgres**；缓存默认 **memory**（无需 Redis 容器）。



## 已登记应用



| app_key | 应用根目录 | CLI | API | Agent | Vite | PG 宿主机 | 数据库 |

|---------|-----------|-----|-----|-------|------|-----------|--------|

| `main` | `menu-master/` | `ams-main` | 7001 | — | 5173 | **5432** | `admin_platform` |

| `novel` | `novel-sub/` | `ams-novel` | 7002 | **7003** | 5174 | **5433** | `novel_db` |



## 容器命名



| 应用 | Postgres | API | Agent | 前端 |

|------|----------|-----|-------|------|

| main | `ams-main-postgres` | `ams-api-main` | — | `ams-main-frontend` |

| novel | `ams-novel-postgres` | `ams-api-novel` | `ams-novel-agent` | `ams-novel-frontend` |



## 子应用注册（主库 subapp_registry）



| microapp_name | app_key | route_prefix | dev entry |

|---------------|---------|--------------|-----------|

| `novel-app` | `novel` | `novel` | `http://localhost:5174` |



菜单 API `/api/menus`、`/api/menus/root` 会附带 `entry`、`subapp` 字段；可用 `SUBAPP_NOVEL_ENTRY` 覆盖。



## 已废弃



- 全仓共享 `ams-postgres` / `deploy/compose/infra.yml`

- 独立 `app_key=agent` 与 `apps/agent-server/`

- `apps/` 目录

- 本地默认 Redis 容器（`ams-*-redis`）



## 新增子应用



1. 复制 `novel-sub/` 或 `menu-master/` 整目录

2. 在本表登记 API/PG 宿主机端口（递增）

3. 在主库 `subapp_registry` + `menu_items` 登记 microapp

4. 按 [`.cursor/rules/subapp-onboarding.mdc`](../.cursor/rules/subapp-onboarding.mdc) 完成接入 checklist

5. 实现完整 `{app}/deploy/docker-compose.yml` 并验证可独立 `ams-{app_key} local`

