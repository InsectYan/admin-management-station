# 小说创作平台代码开发设计报告

## 项目概述

本报告旨在设计一个基于Electron+React+Ant Design+Ant Design Charts的桌面端小说创作平台，该平台将作为微前端(qiankun)架构下的子应用，通过Node.js鸡蛋框架与PostgreSQL数据库构建完整的技术栈。平台将实现小说管理、详情/编辑和新建小说三大核心功能模块，采用分层架构设计，确保系统稳定、安全且易于扩展。**项目采用Electron框架实现跨平台桌面应用，React作为前端框架，结合Ant Design组件库保证UI一致性，qiankun微前端框架实现与主应用的集成，Node.js鸡蛋框架作为后端服务，PostgreSQL作为持久化存储**。整个系统将遵循现代化开发规范，注重性能优化、用户体验和数据安全。

## 技术架构

### 技术栈选择与优势

| 技术组件 | 选择理由 | 主要功能 |
|---------|---------|---------|
| **Electron** | 跨平台支持，原生桌面应用体验，与React生态兼容性好 | 创建桌面应用窗口，管理多进程通信，实现文件系统访问 |
| **React** | 现代化前端框架，组件化开发，丰富的生态系统 | 构建用户界面，管理组件状态，实现交互逻辑 |
| **Ant Design** | 企业级UI组件库，完整的布局组件，与React无缝集成 | 提供标准化UI组件，保证界面一致性，实现美观的UI效果 |
| **qiankun** | 微前端标准解决方案，轻量级，与React/Vue等框架兼容 | 实现子应用与主应用的集成，处理生命周期管理，解决路由冲突 |
| **Node.js** | 轻量级、高性能、异步I/O模型 | 构建RESTful API，处理业务逻辑，连接PostgreSQL数据库 |
| **PostgreSQL** | 强事务支持，JSONB类型，高级查询功能 | 存储小说内容，支持复杂数据结构，保证数据一致性 |

### 系统架构设计

![系统架构设计图](https://example.com/architecture.png)

系统采用分层架构设计，分为以下四个层次：

1. **表现层**：基于React+Ant Design构建用户界面，实现小说管理、详情/编辑和新建小说功能
2. **业务逻辑层**：通过Node.js鸡蛋框架处理业务逻辑，包括小说创建、编辑、删除和查询等操作
3. **数据访问层**：通过TypeORM或Knex.js等ORM工具实现与PostgreSQL数据库的交互
4. **基础设施层**：Electron主进程管理应用生命周期，IPC通信实现前后端交互，qiankun框架实现微前端集成

### 交互流程设计

#### 小说管理交互流程

![小说管理交互流程](https://example.com/novel-management.png)

1. 用户通过左侧菜单点击"小说管理"进入小说列表页面
2. 用户可在顶部筛选栏输入小说名称或选择小说状态进行筛选
3. 系统向后端发送筛选请求，获取分页数据
4. 用户点击"新建"按钮，跳转到新建小说步骤条页面
5. 用户在步骤条页面填写基本信息，点击下一步
6. 用户填写大纲信息，点击下一步
7. 用户填写正文创作信息，点击下一步
8. 用户进行审稿润色，点击完成
9. 系统将小说数据存储到PostgreSQL数据库
10. 用户点击小说列表中的"查看"按钮，跳转到小说详情/编辑页面

#### 新建小说步骤条交互流程

![新建小说步骤条交互流程](https://example.com/creation-process.png)

1. 用户点击"新建"按钮，进入步骤条引导界面
2. 用户填写基本信息（名称、立意等），点击下一步
3. 系统暂存表单数据到electron-store或IPC主进程
4. 用户进入大纲构建界面，系统从暂存数据中加载已填写的信息
5. 用户填写正文创作内容，点击下一步
6. 系统暂存表单数据到electron-store或IPC主进程
7. 用户进入审稿润色界面，系统从暂存数据中加载已填写的信息
8. 用户点击"完成"按钮，系统将所有步骤数据提交到后端
9. 后端验证数据完整性，存储到PostgreSQL数据库
10. 系统返回小说管理列表，显示新创建的小说

## 项目目录结构

项目采用模块化组织方式，目录结构如下：

```
novel-creation-app/
├── src/
│   ├── main/            # Electron主进程
│   │   ├── index.ts      # 主进程入口文件
│   │   ├── main_window.ts # 主窗口管理
│   │   ├── utils/        # 工具函数
│   │   └──...
│   ├── preload/         # 预加载脚本
│   │   ├── index.ts      # 预加载入口
│   │   └──...
│   ├── renderer/        # React子应用
│   │   ├── src/
│   │   │   ├── assets/    # 静态资源
│   │   │   ├── components/ # 公共组件
│   │   │   ├── views/     # 页面组件
│   │   │   ├── stores/     # 全局状态管理
│   │   │   ├── services/   # 服务层
│   │   │   ├── router/     # 路由配置
│   │   │   ├──...          # 其他配置
│   │   │   └── main.jsx     # React入口
│   │   └──...
│   └──...
├── package.json
├── vite.config.ts       # Vite配置
├──...                     # 其他配置
└──...
```

## 通信机制设计

系统采用多种通信机制，确保各组件间高效、安全地交互：

### 1. Electron进程间通信(IPC)

```typescript
// 主进程 - main_window.ts
import { BrowserWindow,ipcMain } from 'electron';

// 创建主窗口
const createMainWindow = () => {
  const win = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      nodeIntegration: false,
      contextIsolation: true,
      preload: path.join(__dirname, 'preload/index.js'),
    },
  });

  // 发送basename到渲染进程
  win.webContents.on('did-finish-load', () => {
    win.webContents.send('basename', '/novel-app');
  });

  // 注册IPC事件监听
  IPCMain.register('get-novel-data', (event, novelId) => {
    // 通过IPC获取小说数据
    const data = getNovelData(novelId);
    event.preventDefault();
    event.sender.send('novel-data-response', data);
  });

  return win;
};
```

### 2. HTTP RESTful API

```typescript
// 后端API接口 - novels.controller.ts
@Controller('novels')
export class NovelsController {
  constructor(private readonly novelsService: NovelsService) {}

  @Get()
  async getNovels(@Query() filters: NovelsFilters): Promise<NovelsDTO[]> {
    return this.novelsService.getNovels filters);
  }

  @Post()
  async createNovel(@Body() novelData: CreateNovelDTO): Promise<NovelDTO> {
    return this.novelsService.createNovel(novelData);
  }

  @Put(':id')
  async updateNovel(@Param('id') id: string, @Body() novelData: UpdateNovelDTO): Promise<NovelDTO> {
    return this.novelsService.updateNovel(id, novelData);
  }

  @Delete(':id')
  async deleteNovel(@Param('id') id: string): Promise<void> {
    return this.novelsService.deleteNovel(id);
  }
}
```

### 3. 微前端(qiankun)生命周期通信

```typescript
// React子应用入口 - main.jsx
import { render } from 'react-dom';
import { App } from './App';
import { emitter, MICRO_EVENT } from './qiankun/eventBus';

// 定义生命周期钩子
export async function bootstrap() {
  console.log('[子应用] 小说创作平台初始化完成');
}

export async function mount(props) {
  console.log('[子应用] 小说创作平台挂载', props);
  // 获取主应用传递的basename
  constbasename = propsbasename ? propsbasename : '/novel-app';
  // 将basename挂载到全局变量
  window.__NOVEL_APPbasename__ =basename;

  // 初始化全局状态
  emitter.on(MICRO_EVENT.GLOBAL State Change, (state) => {
    // 监听全局状态变化
    console.log('[子应用] 全局状态变化:', state);
  });

  // 渲染应用
  render(<App />, props.container.querySelector('#root'));
}

export async function unmount() {
  console.log('[子应用] 小说创作平台卸载');
  // 清理全局状态和事件监听
  emitter offAll();
}
```

### 4. 数据持久化策略

```typescript
// electron-store配置 - stores/novelStore.ts
import Store from 'electron-store';

// 创建小说数据存储实例
export const novelStore = new Store({
  encryptionKey: process.env.ELECTRON_store_ENCRYPTION_KEY, // 使用环境变量加密
  defaults: {
    novels: [],
    draftNovels: []
  }
});

// 获取小说列表
export function getNovels(): Novel[] {
  return novelStore.get('novels');
}

// 保存小说数据
export function saveNovel(novel: Novel): void {
  const novels = getNovels();
  const index = novels.findIndex(n => n.id === novel.id);
  if (index !== -1) {
    novels[index] = novel;
  } else {
    novels.push(novel);
  }
  novelStore.set('novels', novels);
}
```

## 数据库设计

### 表结构设计

#### 小说表(novels)

```sql
-- 创建小说表
CREATE TABLE novels (
  id SERIAL PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  author_id INT NOT NULL REFERENCES users(id),
  status VARCHAR(50) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  plot JSONB, -- 存储小说大纲等复杂结构
  progress INT DEFAULT 0, -- 创作进度百分比
  word_count INT DEFAULT 0, -- 总字数统计
  draft JSONB -- 存储草稿信息
);

-- 创建索引
CREATE INDEX idx_novel_title ON novels (title);
CREATE INDEX idx_novel_status ON novels (status);
CREATE INDEX idx_novel_author ON novels (author_id);
CREATE INDEX idx_novel_plot ON novels USING GIN (plot); -- 为JSONB字段创建GIN索引
```

#### 章节表(chapters)

```sql
-- 创建章节表
CREATE TABLE chapters (
  id SERIAL PRIMARY KEY,
  novel_id INT NOT NULL REFERENCES novels(id) ON DELETE CASCADE,
  title VARCHAR(255) NOT NULL,
  content TEXT NOT NULL,
  chapter_number INT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 创建索引
CREATE INDEX idx_chapter_novel ON chapters (novel_id);
CREATE INDEX idx_chapter_number ON chapters (novel_id, chapter_number);
```

#### 用户表(users)

```sql
-- 创建用户表
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  username VARCHAR(100) NOT NULL UNIQUE,
  password VARCHAR(255) NOT NULL,
  email VARCHAR(255) NOT NULL UNIQUE,
  role VARCHAR(50) NOT NULL DEFAULT 'reader', -- author|reader|admin
  profile JSONB,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 创建索引
CREATE INDEX idx_user_username ON users (username);
CREATE INDEX idx_user_email ON users (email);
CREATE INDEX idx_user_role ON users (role);
```

### 数据库连接配置

```typescript
// 后端数据库连接配置 - database.module.ts
import { Module } from '@守约/nestjs';
import { TypeORMModule } from '@守约/nestjs-typeorm';
import { config } from 'process';

@ConfigurationModule({
  imports: [
    TypeORMModule.forRoot({
      type: 'postgres',
      host: config环境变量 POSTGRES_HOST,
      port: config环境变量 POSTGRES_PORT,
      username: config环境变量 POSTGRES_USER,
      password: config环境变量 POSTGRES_PASSWORD,
      database: config环境变量 POSTGRES_DATABASE,
      entities: ['dist/**/*.entity{.ts,.js}'],
      synchronize: false, // 生产环境禁用
      dropSchema: false,  // 生产环境禁用
      logging: true,        // 生产环境禁用
      poolSize: 10,        // 连接池大小
      poolTimeout: 30000,  // 连接超时时间
      poolRecycle: 3600000 // 连接回收时间
    })
  ]
})
export class DatabaseModule {}
```

### 数据库优化策略

1. **索引优化**：
   - 为频繁查询的字段（如`title`、`status`）创建B-tree索引
   - 为JSONB字段（如`plot`）创建GIN索引，支持复杂查询
   - 为外键字段（如`author_id`）创建索引，加速关联查询

2. **查询优化**：
   - 使用`EXPLAIN ANALYZE`分析查询性能
   - 避免在JSONB字段上使用`->`操作符进行模糊查询
   - 对于频繁更新的字段，使用`@>`操作符进行存在性查询

3. **事务管理**：
   - 对于创建/更新小说等操作，使用事务保证数据一致性
   - 设置合理的事务隔离级别（默认read committed）
   - 对于读多写少的场景，考虑使用物化视图优化查询性能

## 开发设计方案

### 一、项目初始化与配置

#### 1. 安装依赖

```bash
# 初始化Electron+React项目
npm create electron-vite@latest novel-app -- --template react

# 进入项目目录
cd novel-app

# 安装前端依赖
npm install antd antdCharts react-router electron-store
npm install qiankun vite-plugin-qiankun --save-dev

# 初始化后端项目
npx @守约/cli@latest novel-server
cd novel-server
npx @守约/cli@latest generate:module database
npx @守约/cli@latest generate:module novels
```

#### 2. 配置开发环境

**前端配置(vite.config.ts)**：
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import qiankun from 'vite-plugin-qiankun';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [
    react(),
    qiankun('novel-app', {
      useDevMode: true, // 开发环境启用
     纱箱配置: {
        // 可选：自定义沙箱配置
        // 例如：是否启用样式隔离
        // styleIsolation: 'none'
      }
    })
  ],
  build: {
    rollupOptions: {
      input: 'src/main/index.html',
      output: {
        manualChunks: (id) => {
          if (id.includes('node_modules')) {
            return 'vendor';
          }
          return undefined;
        },
      },
    },
  },
  server: {
    port: 3001, // 开发端口
    host: 'localhost',
  }
});
```

**后端配置(.env)**：
```env
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123
POSTGRES_DATABASE=novel_db
POSTGRES synchronize=false
POSTGRES logging=false
```

### 二、Electron主进程与渲染进程集成

#### 1. 主进程初始化

```typescript
// main_window.ts
import { BrowserWindow,ipcMain } from 'electron';
import path from 'path';

// 窗口配置
const createMainWindow = () => {
  const win = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      nodeIntegration: false,
      contextIsolation: true,
      prelaod: path.join(__dirname, 'preload/index.js'),
    },
  });

  // 加载渲染进程
  win.loadFile('index.html');

  // 注册IPC事件
  IPCMain.register('get-novel-data', (event, novelId) => {
    // 通过IPC获取小说数据
    const data = getNovelData(novelId);
    event.preventDefault();
    event.sender.send('novel-data-response', data);
  });

  return win;
};

// 主进程入口
export default class MainProcess {
  private static instance: MainProcess;
  private readonly win: BrowserWindow;

  private constructor() {
    this.win = createMainWindow();
  }

  public static getInstance() {
    if (!MainProcess.instance) {
      MainProcess.instance = new MainProcess();
    }
    return MainProcess.instance;
  }

  public static destroyInstance() {
    if (MainProcess.instance) {
      MainProcess.instance.win.close();
      MainProcess.instance = undefined;
    }
  }
}
```

#### 2. 预加载脚本(preload)

```typescript
// preload/index.js
const { contextBridge, ipcRenderer } = require('electron');

// 暴露IPC通道
contextBridge.exposeInMainWorld('electronIPCS', {
  send: (channel, data) => {
    IPCRenderer.send(channel, data);
  },
  receive: (channel, func) => {
    IPCRenderer.on(channel, (event, ...args) => func(...args));
  }
});

// 暴露API给渲染进程
contextBridge.exposeInMainWorld('api', {
  getNovelData: (novelId) => {
    return new Promise((resolve) => {
      IPCRenderer.send('get-novel-data', novelId);
      IPCRenderer.on('novel-data-response', (event, data) => {
        resolve(data);
      });
    });
  }
});
```

### 三、微前端(qiankun)子应用集成

#### 1. 子应用入口配置

```typescript
// renderer/main.jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import { createRouter, createWebHistory } from 'react-router-dom';
import { emitter, MICRO_EVENT } from './qiankun/eventBus';

// 全局状态管理
export const globalState = {
  novelData: null,
  draftData: {}
};

// 定义生命周期钩子
export async function bootstrap() {
  console.log('[子应用] 小说创作平台初始化完成');
}

export async function mount(props) {
  console.log('[子应用] 小说创作平台挂载', props);
  // 获取主应用传递的basename
  constbasename = propsbasename ? propsbasename : '/novel-app';
  // 将basename挂载到全局变量
  window.__NOVEL_APPbasename__ =basename;

  // 初始化全局状态
  emitter.on(MICRO_EVENT.GLOBAL State Change, (state) => {
    // 监听全局状态变化
    console.log('[子应用] 全局状态变化:', state);
  });

  // 渲染应用
  const root = ReactDOM.createRoot(document.getElementById('root'));
  root.render(<Appbasename={basename}/>);
  return root;
}

export async function unmount() {
  console.log('[子应用] 小说创作平台卸载');
  // 清理全局状态和事件监听
  emitter offAll();
}
```

#### 2. React Router配置

```typescript
// renderer router/index.js
import { createRouter, createWebHashHistory } from 'react-router-dom';
import {basename} from './config';

const routes = [
  { path: '/novels', name: 'NovelList', component: NovelListPage },
  { path: '/novels/new', name: 'NovelCreation', component: NovelCreationPage },
  { path: '/novels/:id', name: 'NovelDetail', component: NovelDetailPage }
];

// 创建路由器
const router = createRouter({
  history: createWebHashHistory(),
  routes: routes.map((route) => ({
    path: route.path,
    element: <RouteElement key={route.name} component={route.component}.basename={basename}/>,
    name: route.name
  })),
 basename:basename // 设置basename
});

export default router;
```

### 四、核心功能模块实现

#### 1. 小说管理页面(NovelListPage)

**组件结构**：
```typescript
import React, { useState, useEffect } from 'react';
import { Table, Input, Button, Space } from 'antd';
import { Steps } from 'antd';
import { Link } from 'react-router-dom';
import { emitter, MICRO_EVENT } from './qiankun/eventBus';

const NovelListPage: React.FC = () => {
  // 状态管理
  const [novels, setNovels] = useState<Novel[]>([]);
  const [filters, setFilters] = useState<NovelFilters>({
    title: '',
    status: ''
  });

  // 加载小说数据
  const loadNovels = async () => {
    try {
      const response = await axios.get('/api/novels', {
        params: filters
      });
      setNovels(response.data);
    } catch (error) {
      console.error('加载小说列表失败:', error);
    }
  };

  // 筛选小说
  const handleFilter = (name: string, value: string) => {
    setFilters({
      ...filters,
      [name]: value
    });
    loadNovels();
  };

  // 表格列定义
  const columns = [
    {
      title: 'ID',
      dataIndex: 'id',
      key: 'id',
      width: 80
    },
    {
      title: '小说名称',
      dataIndex: 'title',
      key: 'title',
      width: 200
    },
    {
      title: '作者',
      dataIndex: 'author',
      key: 'author',
      width: 150
    },
    {
      title: '状态',
      dataIndex: 'status',
      key: 'status',
      width: 120
    },
    {
      title: '进度',
      dataIndex: 'progress',
      key: 'progress',
      width: 120,
      render: (progress) => (
        <Space>
          <Progress percent={progress} />
          <span>{progress}%</span>
        </Space>
      )
    },
    {
      title: '操作',
      key: 'action',
      render: (record: Novel) => (
        <Space size="middle">
          <Button type="primary" onClick={() => handle查看详情(record.id)}>
            查看
          </Button>
          <Button danger onClick={() => handleDelete记录(record.id)}>
            删除
          </Button>
        </Space>
      )
    }
  ];

  return (
    <div className="content-container">
      <div className="filter-section">
        <Input
          placeholder="输入小说名称"
          value={filters.title}
          onChange={(e) => handleFilter('title', e.target.value)}
        />
        <Select
          placeholder="选择状态"
          value={filters.status}
          onChange={(value) => handleFilter('status', value)}
        >
          <Option value="">全部</Option>
          <Option value="draft">草稿</Option>
          <Option value="published">已发布</Option>
          <Option value="archived">归档</Option>
        </Select>
        <Button type="primary" onClick={loadNovels}>
          筛选
        </Button>
        <Button type="dashed" onClick={handleNewNovel} style={{ float: 'right' }}>
          新建小说
        </Button>
      </div>
      <div className="table-section">
        <Table columns={columns} dataSource={novels} rowKey="id" />
      </div>
    </div>
  );
};

export default NovelListPage;
```

**样式配置**：
```css
/* NovelListPage.css */
.content-container {
  padding: 20px;
  max-width: 1200px;
  margin: 0 auto;
}

.filter-section {
  margin-bottom: 20px;
  display: flex;
  gap: 10px;
  align-items: center;
}

.filter-section input {
  width: 200px;
}

.table-section {
  background-color: #fff;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}
```

#### 2. 小说详情/编辑页面(NovelDetailPage)

**组件结构**：
```typescript
// NovelDetailPage.tsx
import React, { useState, useEffect } from 'react';
import { Form, Input, Card, Descriptions, Button, Space } from 'antd';
import { Link, useParams } from 'react-router-dom';
import { emitter, MICRO_EVENT } from './qiankun/eventBus';

const NovelDetailPage: React.FC = () => {
  // 获取路由参数
  const { id } = useParams();

  // 状态管理
  const [novel, setNovel] = useState<Novel | null>(null);
  const [form] = Form.useForm();

  // 加载小说数据
  const loadNovel = async () => {
    if (!id) return;

    try {
      const response = await axios.get(`/api/novels/${id}`);
      setNovel(response.data);
      // 设置表单初始值
      form.setFieldsValue({
        title: response.data.title,
        author: response.data.author,
        status: response.data.status,
        progress: response.data.progress,
        word_count: response.data.word_count
      });
    } catch (error) {
      console.error('加载小说详情失败:', error);
    }
  };

  // 提交表单
  const handleSubmit = async (values: Novel) => => {
    if (!id) return;

    try {
      await axios.put(`/api/novels/${id}`, values);
      message.success('小说信息更新成功！');
      // 刷新数据
      loadNovel();
    } catch (error) {
      console.error('更新小说信息失败:', error);
      message.error('更新小说信息失败，请重试！');
    }
  };

  useEffect(() => {
    loadNovel();
  }, [id]);

  return (
    <div className="novel/B8 detail-container">
      {novel && (
        <div>
          <Card title="小说详情" className="novel-card">
            <Descriptions column={1}>
              <Descriptions.Item label="ID">
                {novel.id}
              </ descriptions .Item>
              < Descriptions .Item label="标题">
                {novel.title}
              </ descriptions .Item>
              < Descriptions .Item label="作者">
                {novel.author}
              </ descriptions .Item>
              < Descriptions .Item label="状态">
                {novel.status}
              </ descriptions .Item>
              < Descriptions .Item label="创建时间">
                {novel.created_at}
              </ descriptions .Item>
              < Descriptions .Item label="更新时间">
                {novelupdated_at}
              </ descriptions .Item>
            </ descriptions s>
          </ Card s>
          <div className="action-section">
            <Button type="primary" onClick={handleSubmit}>
              保存
            </ Button s>
            < Button s type="dashed">
              < Link to="/novels" >返回列表</ Link s>
            </ Button s>
          </ div s>
        </ div s>
      )}
    </ div s>
  );
};

export default NovelDetailPage;
```

#### 3. 新建小说步骤条功能(NovelCreationPage)

**组件结构**：
```typescript
// NovelCreationPage.tsx
import React, { useState, useEffect } from 'react';
import { Steps, Form, Input, Card, Descriptions, Button, Space } from 'antd';
import { Link, useParams, useLocation } from 'react-router-dom';
import { emitter, MICRO_EVENT } from './qiankun/eventBus';
import { useRouteMatch } from 'react-router-dom';

const NovelCreationPage: React.FC = () => {
  // 获取路由参数
  const { id } = useParams();
  const location = useLocation();
  const match = useRouteMatch();

  // 状态管理
  const [currentStep, setCurrentStep] = useState(0);
  const [novelData, setNovelData] = useState<Novel | null>(null);
  const [form] = Form.useForm();

  // 步骤配置
  const steps = [
    {
      title: '基本信息',
      content: (
        <Form form={form} layout="vertical">
          <Form.Item name="title" label="标题" rules={[{ required: true, message: '请输入小说标题' }]}>
            <Input />
          </Form.Item>
          <Form.Item name="author" label="作者" rules={[{ required: true, message: '请输入作者名称' }]}>
            <Input />
          </Form.Item>
          <Form.Item name="status" label="状态" rules={[{ required: true, message: '请选择小说状态' }]}>
            <Select>
              <Option value="draft">草稿</Option>
              <Option value="published">已发布</Option>
              <Option value="archived">归档</Option>
            </Select>
          </Form.Item>
        </Form>
      )
    },
    {
      title: '大纲构建',
      content: (
        <Form form={form} layout="vertical">
          <Form.Item name="plot" label="大纲">
            <Input.TextArea rows={8} />
          </Form.Item>
        </Form>
      )
    },
    {
      title: '正文创作',
      content: (
        <Form form={form} layout="vertical">
          <Form.Item name="content" label="正文">
            <Input.TextArea rows={20} />
          </Form.Item>
        </Form>
      )
    },
    {
      title: '审稿润色',
      content: (
        <Form form={form} layout="vertical">
          <Form.Item name="review" label="审稿意见">
            <Input.TextArea rows={10} />
          </Form.Item>
        </Form>
      )
    }
  ];

  // 提交表单
  const handleSubmit = async (values: Novel) => {
    if (!id) return;

    try {
      await axios.post('/api/novels', values);
      message.success('小说创建成功！');
      // 返回列表
      history.push('/novels');
    } catch (error) {
      console.error('创建小说失败:', error);
      message.error('创建小说失败，请重试！');
    }
  };

  // 处理下一步
  const handleNext = () => {
    if (currentStep < steps.length - 1) {
      setCurrentStep(currentStep + 1);
    } else {
      // 最后一步提交
      handleSubmit(novelData);
    }
  };

  // 处理上一步
  const handlePrevious = () => {
    if (currentStep > 0) {
      setCurrentStep(currentStep - 1);
    }
  };

  // 处理步骤跳转
  const handleStepChange = (step: number) => {
    setCurrentStep(step);
  };

  // 处理表单提交
  const handleFormSubmit = (values: Novel) => {
    setNovelData(values);
  };

  return (
    <div className="novel/ creation-container">
      <Card title="小说创作" className="creation-card">
        <Steps current={currentStep} className="creation-steps">
          {steps.map((step, index) => (
            <Step key={index} title={step.title} />
          ))}
        </Steps>
        <div className="step-content">
          {steps[currentStep].content}
        </div>
        <div className="action-section">
          <Button onClick={handlePrevious} disabled={currentStep === 0}>
            上一步
          </Button>
          <Button type="primary" onClick={handleNext} style={{ float: 'right' }}>
            {currentStep === steps.length - 1 ? '完成' : '下一步'}
          </Button>
        </div>
      </Card>
    </div>
  );
};

export default NovelCreationPage;
```

**样式配置**：
```css
/* NovelCreationPage.css */
 creation-container {
   padding: 20px;
   max-width: 1200px;
   margin: 0 auto;
 }

 creation-card {
   margin-bottom: 20px;
   padding: 20px;
 }

 creation-steps {
   margin-bottom: 30px;
 }

 step-content {
   margin-bottom: 20px;
 }

 action-section {
   display: flex;
   gap: 10px;
   justify-content: flex-end;
 }
```

### 五、Agent集成方案

#### 1. Agent服务接口设计

```typescript
// agent服务能力接口
export interface AgentService {
  generatePlot: (prompt: string) => Promise<string>;
  build大纲: (prompt: string) => Promise<string>;
  reviewContent: (prompt: string) => Promise<string>;
}
```

#### 2. LangChain实现示例

```typescript
// langchain集成示例 - langchain集成.js
import { OpenAI } from "langchain/llms/openai";
import { AgentExecutor, ZeroShotAgent } from "langchain/agents";
import { Tool } from "langchain/tools";
import { SQLDatabase } from "langchain/databases/sql";
import { SQLToolKit } from "langchain/extras/tool_kits/sql";
import { Novel } from "./实体/novel";

// 创建LangChain代理
export const createLangChainAgent = async () => {
  // 初始化OpenAI模型
  const llm = new OpenAI({
    temperature: 0.7,
    openAIKey: process.envLangChain_API_KEY,
    maxTokens: 2000
  });

  // 创建SQL数据库工具
  const db = new SQLDatabase({
    dialect: "postgres",
    host: process.envPOSTGRES_HOST,
    port: process.envPOSTGRES_PORT,
    user: process.envPOSTGRES_USER,
    password: process.envPOSTGRES_PASSWORD,
    database: process.envPOSTGRES_DATABASE
  });

  // 创建SQL工具包
  const sqlToolKit = await SQLToolKit.fromSQLDatabase(db);

  // 创建工具列表
  const tools = [
    new Tool({
      name: "GeneratePlot",
      func: async (prompt) => {
        const response = await llm.call(`生成小说立意：${prompt}`);
        return response;
      },
      description: "生成小说的立意和主题"
    }),
    new Tool({
      name: "Build大纲",
      func: async (prompt) => {
        const response = await llm.call(`构建小说大纲：${prompt}`);
        return response;
      },
      description: "根据立意生成小说大纲"
    }),
    new Tool({
      name: "ReviewContent",
      func: async (prompt) => {
        const response = await llm.call(`审稿润色：${prompt}`);
        return response;
      },
      description: "对小说内容进行审稿和润色"
    })
  ].concat(sqlToolKit.getTools());

  // 创建零次射击代理
  const agent = ZeroShotAgent.fromLLMAndTools(llm, tools, {
    prefix: "你是一个专业的小说创作代理，擅长生成小说立意、构建大纲和审稿润色。",
    suffix: "请根据用户提供的提示，完成以下任务：\n{format instructions}\n{agent action}\n",
    inputVariables: ["plot", "status", "user_input"]
  });

  // 创建代理执行器
  const agentExecutor = AgentExecutor.fromAgentAndTools({
    agent,
    tools,
    maxIteration: 3
  });

  return agentExecutor;
};
```

### 六、前后端交互接口设计

#### 1. 小说管理API

```typescript
// 小说管理控制器 - novels.controller.ts
@Controller('novels')
export class NovelsController {
  constructor(private readonly novelsService: NovelsService) {}

  @Get()
  async getNovels(@Query() filters: NovelsFilters): Promise<NovelsDTO[]> {
    return this.novelsService.getNovels filters);
  }

  @Post()
  async createNovel(@Body() novelData: CreateNovelDTO): Promise<NovelDTO> {
    return this.novelsService.createNovel(novelData);
  }

  @Put(':id')
  async updateNovel(@Param('id') id: string, @Body() novelData: UpdateNovelDTO): Promise<NovelDTO> {
    return this.novelsService.updateNovel(id, novelData);
  }

  @Delete(':id')
  async deleteNovel(@Param('id') id: string): Promise<void> {
    return this.novelsService.deleteNovel(id);
  }

  @Get(':id')
  async getNovel(@Param('id') id: string): Promise<NovelDTO> {
    return this.novelsService.getNovel(id);
  }
}
```

#### 2. Agent服务API

```typescript
// Agent服务控制器 - agents.controller.ts
@Controller('agents')
export class AgentsController {
  constructor(private readonly agentsService: AgentsService) {}

  @Post('plot')
  async generatePlot(@Body() request: AgentRequest): Promise<AgentResponse> {
    return this.agentsService.generatePlot(request);
  }

  @Post('大纲')
  async build大纲(@Body() request: AgentRequest): Promise<AgentResponse> {
    return this.agentsService.build大纲(request);
  }

  @Post('review')
  async reviewContent(@Body() request: AgentRequest): Promise<AgentResponse> {
    return this.agentsService reviewContent(request);
  }
}
```

#### 3. 数据持久化API

```typescript
// 数据持久化服务 - persistence.service.ts
export class PersistenceService {
  async saveNovel(novel: Novel): Promise<void> {
    // 保存到数据库
    await this.novelRepository.save(novel);

    // 保存到electron-store
    novelStore.set(`novel:${novel.id}`, novel);
  }

  async loadNovel(id: string): Promise<Novel | null> {
    // 从electron-store加载
    const storedNovel = novelStore.get(`novel:${id}`);
    if (storedNovel) {
      return storedNovel;
    }

    // 从数据库加载
    const dbNovel = await this.novelRepository One({ where: { id } });
    if (dbNovel) {
      // 存储到electron-store
      novelStore.set(`novel:${id}`, dbNovel);
      return dbNovel;
    }

    return null;
  }
}
```

### 七、项目部署与维护

#### 1. 构建命令

```bash
# 构建前端应用
npm run build

# 构建后端服务
cd novel-server
npm run build
```

#### 2. 部署方案

**Electron桌面应用部署**：
```bash
# 使用electron-builder打包
npx electron-builder --dir

# 构建为生产版本
npx electron-builder
```

**后端服务部署**：
```bash
# 使用Docker部署
docker build -t novel-server .
docker run -d -p 3000:3000 --name novel-server novel-server
```

#### 3. 数据库迁移

```bash
# 初始化TypeORM迁移
npx typeorm migration:generate CreateNovelsTable

# 执行迁移
npx typeorm migration:run
```

### 八、安全与性能优化

#### 1. 安全措施

```typescript
// 密码加密示例 - user.service.ts
import { hash, compare } from 'bcryptjs';

// 加密密码
export const hashPassword = async (password: string): Promise<string> => {
  return hash(password, 10);
};

// 验证密码
export const comparePassword = async (inputPassword: string, hashedPassword: string): Promise < boolean > => {
  return compare(inputPassword, hashedPassword);
};
```

#### 2. 性能优化

```typescript
// 使用防抖优化频繁操作 - utils/debounce.js
import { emitter, MICRO_EVENT } from './qiankun/eventBus';

// 防抖函数
export const debounce = (func: Function, wait: number): Function => {
  let timeout: NodeJS.Timeout | null = null;

  return (...args: any[]) => {
    if (timeout) {
      clearTimeout(timeout);
    }

    timeout = setTimeout(() => {
      func(...args);
    }, wait);
  };
};

// 使用防抖保存小说进度
export const saveNovelProgress = debounce(async (novelId: string, progress: number) => {
  try {
    // 更新数据库中的进度
    await axios.put(`/api/novels/${novelId}/progress`, { progress });

    // 更新electron-store中的进度
    const novel = await novelStore.get(`novel:${novelId}`);
    if (novel) {
      novelStore.set(`novel:${novelId}`, { ...novel, progress });
    }
  } catch (error) {
    console.error('保存小说进度失败:', error);
  }
}, 1000); // 每秒保存一次
```

### 九、微前端(qiankun)主应用集成指南

#### 1. 主应用配置

```typescript
// 主应用注册小说创作平台 - main-app/src/App.jsx
import { registerMicroApps, start } from 'qiankun';
import { emitter, MICRO_EVENT } from './qiankun/eventBus';

// 小说创作平台配置
const novelAppConfig = {
  name: 'novel-app',
  entry: '//localhost:3001', // 小说创作平台开发地址
  container: '#micro-app-container',
  activeRule: '/novels',
  props: {
   basename: '/novels', // 设置basename
    globalState: globalState // 传递全局状态
  }
};

// 注册微应用
registerMicroApps([novelAppConfig]);

// 启动微应用
start({
 纱箱配置: {
    strictStyleIsolation: true, // 启用严格样式隔离
    useWebComponent: true // 使用Web Components实现沙箱
  },
  // 配置微应用生命周期
  lifeCycles: {
    beforeMount: (app) => {
      console.log('[主应用] 小说创作平台挂载前');
    },
    afterMount: (app) => {
      console.log('[主应用] 小说创作平台挂载后');
    },
    beforeUnmount: (app) => {
      console.log('[主应用] 小说创作平台卸载前');
    },
    afterUnmount: (app) => {
      console.log('[主应用] 小说创作平台卸载后');
    }
  }
});
```

#### 2. 路由配置

```typescript
// 主应用路由配置 - main-app/src router/index.js
import { createRouter, createWebHistory } from 'vue-router';

const routes = [
  {
    path: '/',
    name: 'Home',
    component: Home
  },
  {
    path: '/novels',
    name: 'Novels',
    component: MicroAppContainer, // 微应用容器组件
    props: {
      app: 'novel-app' // 对应注册的微应用名称
    }
  },
  {
    path: '/novels/:id',
    name: 'NovelDetail',
    component: MicroAppContainer, // 微应用容器组件
    props: {
      app: 'novel-app' // 对应注册的微应用名称
    }
  }
];

// 创建路由器
const router = createRouter({
  history: createWebHistory(),
  routes,
 basename: '/' // 主应用basename
});
```

### 十、测试与调试策略

#### 1. 单元测试

```typescript
// 小说服务单元测试 - novels.service.spec.ts
describe('NovelsService', () => {
  let novelsService: NovelsService;
  let novelRepository: NovelRepository;

  beforeAll(async () => {
    // 初始化测试环境
    const moduleRef = await Test.createTestingModule({
      imports: [DatabaseModule],
      providers: [NovelsService, NovelRepository]
    });
    novelsService = moduleRef.get(NovelsService);
    novelRepository = moduleRef.get(NovelRepository);
  });

  afterAll(() => {
    // 清理测试环境
    moduleRef.close();
  });

  describe('getNovels', () => {
    it('应返回所有小说', async () => {
      // 模拟数据库查询结果
      novelRepository.find = jest.fn().mockImplementation(() => {
        return Promise.resolve([
          { id: 1, title: '小说1', status: 'draft' },
          { id: 2, title: '小说2', status: 'published' }
        ]);
      });

      const result = await novelsService.getNovels ({});
      expect(result).TORelativePath('have').TORelativePath('length').TORelativePath('equal')(2);
      expect(result[0].title).TORelativePath('equal')('小说1');
      expect(result[1].title).TORelativePath('equal')('小说2');
    });
  });
});
```

#### 2. 调试工具

**Electron调试**：
- 使用`electron --debug`启动调试模式
- 在Chrome开发者工具中连接到`localhost:9222`

**qiankun调试**：
- 启用`vite-plugin-qiankun`的开发模式
- 使用浏览器控制台查看qiankun日志
- 使用Chrome DevTools的Network选项卡查看微前端加载情况

**PostgreSQL调试**：
- 使用`pgAdmin`或`DBeaver`管理数据库
- 启用PostgreSQL的`log statements`配置记录SQL执行
- 使用`EXPLAIN ANALYZE`分析查询性能

## 总结

本报告详细设计了一个基于Electron+React+Ant Design+Ant Design Charts的桌面端小说创作平台，该平台作为微前端(qiankun)架构下的子应用，通过Node.js鸡蛋框架与PostgreSQL数据库构建完整的技术栈。**系统采用分层架构设计，确保各组件职责清晰，便于维护和扩展**。前端使用React+Ant Design构建用户界面，结合qiankun实现微前端集成；后端使用Node.js鸡蛋框架处理业务逻辑，通过TypeORM或Knex.js连接PostgreSQL数据库；Electron主进程管理应用生命周期，IPC通信实现前后端交互；PostgreSQL数据库采用JSONB类型和GIN索引优化复杂数据存储与查询。

**项目通过模块化设计和标准化接口，确保了开发效率和代码质量**。微前端(qiankun)架构使小说创作平台能够无缝集成到现有主应用中，同时保持独立开发和部署的能力。Electron框架提供了跨平台桌面应用的开发能力，结合React的组件化开发模式，能够快速构建高质量的用户界面。Node.js鸡蛋框架作为后端服务，提供了轻量级、高性能的API服务，PostgreSQL数据库则为复杂的小说数据提供了强大的存储和查询能力。

**未来可扩展方向包括：集成Agent服务实现智能创作辅助、添加版本控制功能支持多人协作、实现离线创作与同步功能、添加数据分析模块可视化创作进度**。这些扩展将使小说创作平台更加智能化和用户友好，满足现代创作需求。