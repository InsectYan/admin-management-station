# 运维子应用 Docker 默认（可提交；路径须跨 Windows / Linux / ECS）
# 端口注册：docs-project/应用端口与命名注册表.md

POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123
POSTGRES_DB=ops_db

OPS_PORT=5203
OPS_POSTGRES_DB=ops_db
OPS_VITE_PORT=5103
OPS_POSTGRES_PORT=5303

JWT_SECRET=CHANGE_ME_LOCAL_JWT
CACHE_DRIVER=memory
MENU_MASTER_URL=http://host.docker.internal:5200
OPS_INTERNAL_KEY=CHANGE_ME_LOCAL_JWT

# Postgres 镜像；国内 Hub 慢时可临时改为镜像站（须保证该 tag 仍存在）
POSTGRES_IMAGE=postgres:16-alpine
# 构建 api-ops 时装 git：Alpine 官方 CDN 不通时换源
ALPINE_MIRROR=https://mirrors.aliyun.com/alpine

# 宿主机 Agent 平台（容器内用 host.docker.internal）
AGENT_PLATFORM_URL=http://host.docker.internal:4001
AGENT_PLATFORM_TIMEOUT_MS=600000
OPS_PROJECT_SKILL=ops-project-skill

# AgentRun：宿主机目录 → 容器 /host-projects（路径相对本文件所在 deploy/）
# 默认挂 monorepo 根（admin-management-station/）。ECS / Linux 勿写 E:/...
HOST_PROJECTS_ROOT=/host-projects
HOST_PROJECTS_MOUNT=../../..
# Windows 本机若要挂盘符下更多工程（如 E:/AI Tools/projects），取消下一行注释并注释掉上一行：
# HOST_PROJECTS_MOUNT=E:/AI Tools/projects

# AgentRun 执行面：auto = 容器能 TLS→阿里云则直连，否则走宿主机 :1329
OPS_DEPLOY_EXECUTOR=auto
OPS_DEPLOY_WORKDIR=/tmp/ops-deploy
# 相对 deploy/ → ops-sub/.ops-deploy-work（勿写 Windows 绝对路径，否则 ECS 起不来）
OPS_DEPLOY_WORKDIR_HOST=../.ops-deploy-work
OPS_HOST_RUNNER_HOST=host.docker.internal
OPS_HOST_RUNNER_PORT=1329
# 旧 CONNECT 代理默认关闭
OPS_HOST_EGRESS_ENABLED=0
NPM_CONFIG_REGISTRY=https://registry.npmmirror.com
