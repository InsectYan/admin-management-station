# 运维管理平台架构关系图

> 子应用 `app_key=ops` · BFF `:5203` · Vite `:5103` · PG `:5303` / `ops_db`  
> 配置生成接 Agent：`ops-project-skill`（`agent-management-master/plugins/ops-project-skill/`）

**图例**：<span style="color:#2563EB">■ 前端</span> · <span style="color:#16A34A">■ BFF</span> · <span style="color:#D97706">■ 数据库</span> · <span style="color:#64748B">■ 主应用</span>

---

## 1. 在 AMS 中的位置

```mermaid
%%{init: {'theme':'base', 'themeVariables': {'fontSize':'20px'}}}%%
flowchart TB
    classDef main fill:#DBEAFE,stroke:#2563EB,stroke-width:2px,color:#1E3A8A
    classDef sub fill:#DCFCE7,stroke:#16A34A,stroke-width:2px,color:#14532D
    classDef db fill:#FEF3C7,stroke:#D97706,stroke-width:2px,color:#78350F
    classDef agent fill:#EDE9FE,stroke:#7C3AED,stroke-width:2px,color:#4C1D95

    MM["menu-master<br/>:5100/:5200"]:::main
    MM -->|"/media/ops"| Ops["ops-sub 前端 :5103"]:::sub
    Ops -->|"http://localhost:5203/api"| BFF["ops-sub BFF :5203"]:::sub
    BFF --> PG[("ops_db :5303")]:::db
    BFF -->|"POST /api/skills/ops-project-skill/invoke"| AG["ops-project-skill :4001"]:::agent
```

Skill 源码位置（持续优化只改这里）：`agent-management-master/plugins/ops-project-skill/`

## 2. 前端导航

| 路由名 | 路径 | 页面 |
|--------|------|------|
| `ops-list` | `/projects` | 项目信息列表：筛选、看板/表格、导出模板、导入、新建 |
| `ops-create` | `/projects/new` | 新建：基础信息 + 生成配置 + 导入项目库 |
| `ops-detail` | `/projects/:id` | 详情只读：基础信息 / 目录 / 路由 / 功能流程 |
| `ops-edit` | `/projects/:id/edit` | 编辑：可改、可重新生成配置 |

嵌入主应用 basename = `/media/ops`。独立 dev 同样使用该 basename，便于与 Qiankun 路径一致。

## 3. API 域

| 方法 | 路径 | Controller |
|------|------|------------|
| GET | `/api/health` | health.index |
| GET | `/api/projects` | project.index |
| GET | `/api/projects/template` | project.template |
| POST | `/api/projects/import` | project.importFile |
| POST | `/api/projects/generate` | project.generate |
| POST | `/api/projects/:id/generate` | project.generateApply |
| GET | `/api/projects/:id/export` | project.exportFile |
| POST | `/api/projects` | project.create |
| GET | `/api/projects/:id` | project.show |
| PUT | `/api/projects/:id` | project.update |
| DELETE | `/api/projects/:id` | project.destroy |

响应信封：`{ code: 0, message, data }`。

## 4. 数据库

```mermaid
%%{init: {'theme':'base', 'themeVariables': {'fontSize':'20px'}}}%%
erDiagram
    OPS_PROJECTS {
      int id PK
      string name
      string project_type
      string status
      text description
      string repo_url
      string source_path
      jsonb directory_tree
      jsonb routes
      jsonb flows
      jsonb extra_json
      timestamptz created_at
      timestamptz updated_at
    }
```

目录树、路由、流程图均存 JSONB，与导出模板字段一一对应。

## 5. 核心用户流

```mermaid
%%{init: {'theme':'base', 'themeVariables': {'fontSize':'20px'}}}%%
flowchart LR
    classDef ui fill:#DBEAFE,stroke:#2563EB,stroke-width:2px,color:#1E3A8A
    classDef bff fill:#DCFCE7,stroke:#16A34A,stroke-width:2px,color:#14532D

    A["新建 / 填路径"]:::ui --> B["生成配置"]:::ui
    B --> C["POST /api/projects/generate"]:::bff
    C --> D["ops-project-skill"]:::bff
    D --> E["导入项目库"]:::ui
    E --> F["详情只读 / 编辑重生成"]:::ui
```

## 6. 分层

| 层 | 职责 |
|----|------|
| 前端 | Vue 3 + Element Plus + AntV X6；森林风令牌挂在 `.ops-sub-root` |
| BFF | Egg.js + Sequelize；启动 `schemaSync`；`agentProxy` 转发生成 |
| DB | 独立 `ops_db` |
| Agent | `agent-management-master/plugins/ops-project-skill/` 扫描路径并产出配置 |
