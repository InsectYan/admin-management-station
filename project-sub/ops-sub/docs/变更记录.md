## [2026-09-19] 公网访问自动改写 localhost

- [UI] 主应用 Qiankun entry、ops/novel/testgen 的 API 基址：浏览器用公网 IP/域名打开时，自动把 `localhost`/`127.0.0.1` 换成当前 hostname（端口不变），ECS 无需手写公网地址

## [2026-09-15] 部署配置表单递归更新

- [Fix] `OpsDeployProductForm` 双向 watch 在 emit 新对象后又 merge 回本地，触发 Maximum recursive updates（报在 PageShell）。改为快照去重 + applying 闸门

## [2026-09-15] 部署产品下拉与配置中文说明

- [Fix] 部署产品目录写死在代码，**不入库**。接口失败时用内置选项，避免 AgentRun 下拉空白
- [UI] 阿里云主账号 UID 标明必填：是主账号约 16 位数字，不是 RAM 子用户 ID
- [UI] INTERNAL_API_KEY 等环境变量旁增加中文用途说明

## [2026-09-15] 风险标注与节点类型分离

- [契约] 节点 `type` 只表示业务形态；`severity` 是独立标注（任意节点都可标红/橙）；`description` 写特点，`risk_note` 写原因与后果
- [UI] 图例拆成类型 + 标注；侧栏在选了标注后出现「风险说明」
- [Skill] 不再生成 `type=risk|warning`，把标注打在真正的 api/action/decision 上

## [2026-09-15] 流程图风险/警告 + Agent 详细梳理

- [UI] 节点 `risk` 红、`warning` 橙，加粗描边；判断改为青蓝以免和警告撞色；工具栏图例；可选 `severity`
- [API] `ops-project/v1` 节点类型与 `extra_json.risks` 反查表；生成结果保留 `extra_json`；Agent/后端不再清空 HTTP 路由
- [Skill] `ops-project-skill` 1.5.0：扫描 `.pi`（skills / message_type / Express HTTP）；总览画全气泡类型；疼伤闸门、医学边界、删除两轮、wipe 落成红节点
- [Fix] 目录扫描不再跳过 `.pi`；有 `workspaces-templates` 时判定为 agent（不被 express 误判成 backend）

## [2026-09-15] 流程图风险/警告节点

- [UI] 节点 `risk` 红、`warning` 橙，加粗描边；判断改为青蓝以免和警告撞色；工具栏图例；可选 `severity`
- [API] `ops-project/v1` 节点类型与 `extra_json.risks` 反查表
- [Skill] `ops-project-skill` 1.4.0：分析时把风险/警告插入图节点（失败终端红、鉴权橙、wipe 风险门）；总览只挂门卫星，不堆每个失败终态

## [2026-09-14] 按产品配置部署（AgentRun 首发）

- [API] 项目 `deploy_config`；`GET /api/deploy/products`；创建任务时快照产品配置
- [UI] 部署页「部署配置」：选 AgentRun / 阿里云 ECS / 腾讯云 CVM / 通用构建
- [Runner] AgentRun：把 AK 写入隔离 `~/.s`、把 `.env.prod` 写入工作区，执行 `node deploy/scripts/run.mjs prod`（`fitness-cli prod` 等价）
- [Demo] ECS / 腾讯云只落配置 + mock
- [Infra] 镜像加 bash 与 `@serverless-devs/s`；可选挂 `HOST_PROJECTS_ROOT`

## [2026-09-13] CR + AUTH-1 + Skill 1.3.0

- [CR] 导入 SSE 自定义事件改为 `fail`，避免与 EventSource 原生 `error` 冲突；导入 payload 在事务提交后再 stash
- [API] menu-master：用户审批/禁用/改角色、管理员重置密码、审计日志、TOTP MFA（两角色政策层，不引入 Casbin）
- [UI] 主应用 `/users` `/audit` `/settings`，登录支持二次验证；注册开通改走用户管理页
- [Skill] `ops-project-skill` 1.3.0：Vue 嵌套 children、Egg `router.*`、页面文件兜底；后端路由保留；分流程超过 10 条按前缀合并
- [DOC] AUTH-1 设计稿已删

## [2026-09-13] P2-1 导入进度 + P2-2 流程图增强

- [API] `ops_import_jobs` + `POST /api/projects/import-jobs` + SSE；小文件仍走同步 `/import` 并弹进度
- [UI] 流程图撤销/重做、对齐、导出 PNG；只读可导出不可改库
- [DOC] 对应设计稿已删

## [2026-09-13] 删已落地设计稿

- [DOC] `docs/design/` 去掉 AUTH-0 / AUTH-OPS / D0–D5 方案；只留 P2-1、P2-2、AUTH-1

## [2026-09-13] 导航补齐与测试平台森林风

- [UI] ops 侧栏第二项「部署任务」`/deploy-jobs` + `GET /api/deploy/jobs/summary`；NavMenu 改读 `navMenu.js`
- [UI] 列表 / 部署 / 历史增加「任务总览」入口
- [UI] novel NavMenu 改读 `navMenu.js`（仍一项「小说列表」）
- [Style] testgen 对齐小说森林令牌 / EP 覆写 / 分页面景观，去掉侧栏 `#409eff`

## [2026-09-13] api-ops 构建改 Alpine 国内源

- [Infra] `Dockerfile.backend` 装 git 默认走 `mirrors.aliyun.com`（官方 CDN TLS 失败）

## [2026-09-13] CR 修复 + D4 runner

- [Fix] 同一项目进行中任务部分唯一索引 + 事务占坑（并发第二单 409）
- [Fix] 删项目先清历史 job（进行中仍 409；有历史可删）
- [Fix] SSE 订阅后再读一次终态，避免错过 `end`
- [Fix] `patchStatus` / `claimRunning` 带上 `project_type`，agent 不再误查 `dist`
- [Fix] 命令白名单不把 `=>` 当成重定向
- [API] `DeployRunnerService`：白名单命令、git clone tag、可 kill；本地路径仍 mock
- [Skill] `ops-deploy-skill` 合同目录（与配置生成分开）
- [Verify] HTTPS `isaacs/once@v1.4.0` + `node -e` → success；`rm` / `exit(1)` → failed；中止 → aborted

## [2026-09-13] 进度写入导航并做代码审查

- [DOC] `design/readme.md`、架构关系图、`项目评分与后续计划.md` 标明已落地 vs 未做
- [CR] 见会话对照：并发占坑、删项目外键、SSE token 查询串、终态竞态

## [2026-09-13] D0–D3 部署表 / 操作页 / 黑窗口 / 历史

- [DB] `ops_deploy_jobs` · `ops_deploy_log_lines` · migration `002_ops_deploy.sql`
- [API] git-tags、创建/列表/详情/日志/导出/SSE/重试/中止；`triggered_by` 写登录名
- [API] `DeployMockService` 灌假日志并走到 success（`OPS_DEPLOY_MOCK=0` 可关）
- [UI] `/projects/:id/deploy` · `/deploy/:jobId` · `/deploy-logs`；列表/详情入口「部署」

## [2026-09-13] AUTH-0 / AUTH-OPS 登录与接入身份

- [API] menu-master：`platform_users` + `/api/auth/register|login|logout|me`，种子 `admin` / `admin123`
- [UI] 主应用 `/login` `/register`，未登录进壳或 `/media/*` 跳登录；侧栏显示用户名与退出
- [API] ops-sub：`sessionAuth` 验 JWT（`JWT_SECRET` 与主应用相同）；`GET /api/health`、模板可匿名
- [UI] ops axios 带 Bearer，401 回主应用登录；独立 `:5103` 用 `ams_token` 交接或 `VITE_OPS_DEV_TOKEN`

## [2026-09-13] 登录优先于部署

- [DOC] design：AUTH-0 登录注册 + AUTH-OPS 接入身份先于 D0；完整审批/Casbin 仍后置

## [2026-09-13] 待做页面设计文档

- [DOC] `docs/design/readme.md` 导引与进度；部署四页 + 数据/Agent/导入进度/流程图增强/权限边界分篇

## [2026-09-12] 项目信息：详情/编辑分流与配置生成

- [UI] 列表标题改为「项目信息」；详情只读、编辑可改；新建页填基础信息并可生成配置后导入
- [UI] 目录树 / 路由表均可「进入流程」；固定 `overview` 总览 + 页面分流程
- [DB] `ops_projects.source_path` · migration `001_add_source_path.sql`
- [API] POST `/api/projects/generate` · POST `/api/projects/:id/generate`
- [Skill] `agent-management-master/plugins/ops-project-skill/`（持续优化入口，BFF 只转发）

## [2026-09-12] 导出模板 startTime 报错

- [Bugfix] 列表「导出模板」点击后 `Cannot read properties of undefined (reading 'startTime')`：下载改走宿主 document、阻止 `<a>` 冒泡成路由，并延后 `revokeObjectURL`

## [2026-09-12] Docker 联调验证

- [Infra] `ams-ops-postgres` 使用 `POSTGRES_IMAGE`（国内镜像 `docker.1ms.run`）映射 `:5303`
- [API] `/api/health`、template、list、import/export 已在宿主机 BFF `:5203` 验证；空库自动写入 2 条演示项目
- [UI] Vite `:5103` 可访问 `/media/ops/projects`

## [2026-09-11] 新增运维子应用

- [Infra] 登记端口 Vite 5103 / BFF 5203 / PG 5303 · `ops_db` · `ams-ops`
- [DB] 新增表 `ops_projects` · `database/init.sql` · 启动 schemaSync + 演示种子
- [API] GET/POST/PUT/DELETE `/api/projects*` · template / import / export
- [UI] `/projects` 列表（导出模板、导入 JSON）· `/projects/:id` 详情目录/路由/AntV X6 流程图
- [Style] 复用小说平台森林风，作用域 `.ops-sub-root` / `--ops-*`
