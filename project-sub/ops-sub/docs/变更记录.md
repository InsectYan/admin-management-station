## [2026-09-12] 导出模板 startTime 报错

- [Bugfix] 列表「导出模板」点击后 `Cannot read properties of undefined (reading 'startTime')`：下载改走宿主 document、阻止 `<a>` 冒泡成路由，并延后 `revokeObjectURL`

## [2026-09-12] Docker 联调验证

- [Infra] `ams-ops-postgres` 使用 `POSTGRES_IMAGE`（国内镜像 `docker.1ms.run`）映射 `:5303`
- [API] `/api/health`、template、list、import/export 已在宿主机 BFF `:5203` 验证；空库自动写入 2 条演示项目
- [UI] Vite `:5103` 可访问 `/media/ops/projects`

## [2026-09-11] 新增运维子应用

- [Infra] 登记端口 Vite 5103 / BFF 5203 / PG 5303 · `ops_db` · `ams-ops`
- [DB] 新增表 `ops_projects` · `database/init.sql` · 启动 schemaSync + 演示种子
- [API] GET/POST/PUT/DELETE `/api/projects*` · template / import / export
- [UI] `/projects` 列表（导出模板、导入 JSON）· `/projects/:id` 详情目录/路由/AntV X6 流程图
- [Style] 复用小说平台森林风，作用域 `.ops-sub-root` / `--ops-*`
