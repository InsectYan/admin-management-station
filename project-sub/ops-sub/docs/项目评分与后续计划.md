# 项目评分与后续计划

评分相对完整运维规划（含部署与薄登录）。导航与进度：[design/readme.md](./design/readme.md)。

| 维度 | 分数 | 说明 |
|------|------|------|
| 子应用接入 | 8 | manifest / Qiankun / 端口登记 / CLI 已齐 |
| 列表与模板 | 8 | 导出模板、导入、导出配置、筛选排序 |
| 详情与图表 | 8 | 目录树、路由、X6 流程图可编辑；撤销/对齐/导出 PNG |
| 样式对齐小说 | 8 | 同一套森林令牌与布局骨架，前缀 `ops-*` |
| Agent / 部署 | 7.5 | 配置生成已接 Skill；部署 UI + mock；BFF runner 真拉 tag/构建（白名单） |
| 权限 / 审批 | 7.5 | 主应用审批/禁用/角色/审计/MFA；两角色政策层（不做 Casbin） |

**综合**：约 **8.0 / 10**。

## 已落地（勿再当缺口）

| ID | 项 | 入口 |
|----|----|------|
| AUTH-0 | 登录 / 注册 JWT | 主应用 `/login` `/register`，种子 `admin` / `admin123` |
| AUTH-OPS | ops 带 Bearer、401 回登录 | 子应用拦截器 + `sessionAuth` |
| D0 | 部署表与 API / SSE | `ops_deploy_jobs` · `ops_deploy_log_lines` |
| D1 | 部署操作页 | `/projects/:id/deploy`（列表/详情按钮） |
| D2 | 黑窗口（mock 日志） | `/projects/:id/deploy/:jobId` |
| D3 | 项目部署历史 | `/projects/:id/deploy-logs` |
| D4 | 真执行（BFF runner） | HTTPS 仓走 git/构建；本地路径仍 mock；验收已勾 |
| D5 | 跨项目任务总览 | `/deploy-jobs` + 侧栏「部署任务」 |
| P2-1 | 导入进度 | 列表导入对话框 + SSE / 小文件同步假进度 |
| P2-2 | 流程图增强 | 撤销/重做/对齐/导出 PNG |
| AUTH-1 | 用户审批 / 角色 / MFA / 审计 | 主应用 `/users` `/audit` `/settings` |

## 后续（还没做）

| 优先级 | ID | 项 |
|--------|----|----|
| P1 | — | 持续优化 `ops-project-skill` 扫描与流程图质量 |
