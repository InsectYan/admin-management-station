# 项目评分与后续计划

评分仅覆盖本期交付范围（项目管理可视化，不含 Agent 部署）。

| 维度 | 分数 | 说明 |
|------|------|------|
| 子应用接入 | 8 | manifest / Qiankun / 端口登记 / CLI 已齐 |
| 列表与模板 | 8 | 导出模板、导入、导出配置、筛选排序 |
| 详情与图表 | 7 | 目录树、路由、X6 流程图可编辑；画布交互可继续打磨 |
| 样式对齐小说 | 8 | 同一套森林令牌与布局骨架，前缀改为 `ops-*` |
| Agent / 部署 | 0 | 按需求本期不做 |
| 权限 / 审批 | 0 | 沿用主应用菜单，不在子应用重复造用户体系 |

**综合**：约 **6.2 / 10**（相对完整运维规划）。

## 后续

| 优先级 | 项 |
|--------|----|
| P0 | 与主应用联调验收：独立 `:5103` + 嵌入 `:5100/media/ops` |
| P1 | 部署管理 UI（版本、日志黑窗口）— 需 Agent 时再接 `agent-management-master` |
| P2 | 用户审批 / Casbin（若主应用权限不够再下沉） |
| P2 | 流程图撤销、对齐、导出 PNG |
