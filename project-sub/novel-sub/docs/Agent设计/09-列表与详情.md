# 09 列表与详情

> 页面：`NovelListPage`、`NovelEditDialog`、`NovelDetailPage`  
> `feature_key`：`quick_edit`（弹窗快编）；详情补全 **不新建 feature**，跳向导复用 03～07

---

## 1. 前端

**列表**

- 工具栏「AI 开书」→ 08 Dialog
- 空状态同一入口
- 筛选区、批量删除：**无 AI**

**编辑弹窗**

- 字段仍是现有表单
- 底部塞折叠坞，`feature_key=basic`（与向导基础信息 **共享会话组**，见 03），不要 `quick_edit` 再拆一组，除非产品要「快编草稿不影响向导会话」——默认共享
- 弹窗矮：坞默认一行，展开后出现主题 Tab

**详情**

- 只读，不在 Tab 里塞输入框
- 每模块「用 AI 补全」→ `/novels/create?id=&step=&ai=1`
- 向导打开时坞自动展开，不自动发送
- 「补全全部缺口」→ 带 `ai=plan` 进 08（创建页顶上出计划坞）

封面：创建步 1 / 快编弹窗「AI 生成封面」，走侧栏多模态图片模型，不走文本 LLM。进度条：人工字段。

---

## 2. 服务端

无新资源。开书走 08；快编走 01 turns + 现有 PUT 小说基础字段。

---

## 3. Agent

无独立 Skill。快编 = 03 的 brainstorm + `fill_basic`。详情补全 = 对应步 Skill。

---

## 4. 验收

- [ ] 详情页无直连 `:4001` 的请求
- [ ] 补全跳转后 `?ai=1` 坞展开，表单仍是已保存数据
- [ ] 快编应用后未点弹窗保存，列表数据不变
