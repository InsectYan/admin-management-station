## [2026-08-29] 坞会话删除
- [API] `DELETE /api/ai/sessions/:id`，先删消息再删会话；migration `004` 把 FK 改回 ON DELETE CASCADE
- [UI] 左侧坞「新开」旁加「删除」，确认后删当前会话组；删空则自动新开一组

## [2026-08-29] 多模态模型 + 封面出图
- [Agent] `app/lib/media` catalog（图/视频能力标签）；`GET /api/media/profiles`、`POST /api/media/generate`
- [主应用] 侧栏「多模态」与文本「AI 模型」并列，`ams-media-profile-id` 全局
- [API] `POST /api/ai/cover/generate` 走选中图片模型，不经 writer；结果写 `cover_url` 待保存
- [Doc] 规范 `media-model.mdc`：非文本调用一律该选项

## [2026-08-29] 列表快编坞 + 详情 AI 补全
- [UI] 编辑弹窗底部折叠坞，`feature_key=basic` 与向导共享会话；应用到表单后须点保存才改列表
- [UI] 详情「用 AI 补全」跳 `/novels/create?id=&step=&ai=1`，坞展开不自动发送
- [UI] 「补全全部缺口」带 `ai=plan` 打开计划坞；无新 Skill / 接口

## [2026-08-29] 总任务拆分 + 列表 AI 开书
- [Agent] `novel-orchestrator-skill`：只出 `tasks`，不写正文、不 invoke writer
- [API] registry `orchestrate.*`；`POST /api/ai/dispatch` 再调目标 feature turns；人物在世界观完成前 409
- [UI] 列表「AI 开书」Dialog；执行下一步跑 `fill_basic` 并创建草稿后进入向导

## [2026-08-29] 创建步 5 内容组织 AI 坞
- [API] registry `content.*`；无 novel_id 返回 `NOVEL_REQUIRED`；faction 白名单；patch 不含 order
- [Agent] writer `fill_chapters`；默认不走 brainstorm
- [UI] 步 5 挂竖栏坞；按 title 合并追加；只勾阵营时不改标题

## [2026-08-29] 创建步 4 大纲 AI 坞
- [API] registry `outline.*`；无 novel_id 返回 `NOVEL_REQUIRED`；字数超篇幅只 warning
- [Agent] writer `fill_outline`；brainstorm `focus=plot`
- [UI] 步 4 挂竖栏坞；空树替换，已有卷按标题合并/追加

## [2026-08-29] 创建步 3 人物 AI 坞
- [API] registry `characters.*`；无 novel_id 返回 `NOVEL_REQUIRED`；role/relation 白名单
- [Agent] writer `fill_characters`；brainstorm `focus=cast`
- [UI] 步 3 挂竖栏坞；按姓名合并角色；边把姓名解析成 id；选中角色提示「正在补全」

## [2026-08-29] 创建步 2 世界观 AI 坞
- [API] registry `world.*`；无 novel_id 返回 `NOVEL_REQUIRED`；patch 含文本字段与 timeline
- [Agent] writer `fill_world`；brainstorm 支持 world focus
- [UI] 步 2 挂竖栏坞；未保存草稿禁用发送；时间轴按 year+event 追加去重

## [2026-08-29] AI 坞：SSE 思考流 + 选项卡禁选
- [API] `POST /api/ai/sessions/:id/turns/stream` 转发 Agent invoke-stream；思考 delta 推前端，完成后折叠
- [UI] 类型/题材/篇幅/读者/节奏场景卡禁用；patch 只含 title / 立意 / 简介
- [Agent] Loop 在流式 invoke 时改走 `llmChatStream`

## [2026-08-29] 正文与 Agent 对话 Markdown 渲染
- [UI] 引入 `markdown-it` + `DOMPurify`；共用 `NovelMarkdown`
- [UI] 详情立意/简介/世界观/人物长文、创建预览、坞内对话改为 Markdown
- [Doc] AMS 规则 `novel-sub-development.mdc`

## [2026-08-28] Agent 首批落地（会话 + turns + 竖栏坞 + 步 1）
- [DB] `novel_ai_sessions` / `novel_ai_messages` · migration `003_novel_ai_sessions.sql`
- [API] `/api/ai/sessions` CRUD、`/turns` 按 `scene` 分发 brainstorm→writer、`/apply` 只标记 applied
- [Agent] `plugins/novel-brainstorm-skill`、`plugins/novel-writer-skill`（loop 单步 JSON patch）
- [UI] 创建步 1 竖栏 `AiFormDock`：场景卡、对话、应用到表单（不自动保存）
- [Tool] `npm run agent:smoke`

## [2026-08-28] Agent 首批开发计划（待确认）
- [Doc] `docs/Agent组件开发进度/readme.md`：Agent 从 writer-skill 起；BFF 唯一 turns + scene 分发；先做竖栏坞 + 创建步 1

## [2026-08-28] 表单 AI 坞改为竖栏 + 场景卡树
- [Doc] `docs/Agent设计/02-表单AI坞.md`：默认贴左侧菜单竖栏；`scenes[]` 父/子数据插入；对话按多轮聊天；可切底栏（图标条 + 浮层）

## [2026-08-28] Agent 三端明细（一组件一文件）
- [Doc] `docs/Agent设计/00-组件拆分.md` 起：会话、表单AI坞、五步+开书+列表，及 3 个 SKILL
- [Doc] 主交互改为表单底部坞 + 本次主题 Tab + `novel_id+feature_key` 会话；总览 readme 增加目录

## [2026-08-28] 小说平台 Agent 设计稿
- [Doc] 产品入口 `docs-project/小说平台Agent.md`（原文为空，按现网页面重建）
- [Doc] `docs/Agent设计/readme.md`：4 个复用 AI 组件、orchestrator / writer 分工、字段映射、功能向导、Phase 0～4
- [Doc] `docs/架构关系图.md` 增补 Agent 调用关系（设计稿，代码未实现）

## [2026-08-28] 小说枚举表与级联选择
- [DB] 新增 `genre_categories` / `genre_subcategories` / `theme_categories` / `audience_categories` / `length_categories` / `update_paces` / `novel_themes`；`novels` 增加对应外键 · migration `002_novel_enums.sql` · 启动 seed
- [API] `GET /api/novel-enums` 返回类型树、题材（按热度）、读者、篇幅、更新节奏；小说 CRUD 写入外键并回填名称
- [UI] 创建/编辑：小说类型级联选择，题材多选可搜索，篇幅/读者/节奏下拉改为枚举；列表筛选与详情展示同步

## [2026-08-28] 子应用切换样式丢失与 #app 上方留白
- [Fix] 切到测试平台再回小说样式被卸掉：Docker 内 Vite `data-vite-dev-id` 均为 `/app/src/...`，后一个子应用按同一 id 覆盖 `<style>`；`qiankunStyleKeeper` 改为认领/快照后卸下本应用样式，再次 mount 还原。主应用 `beforeLoad` 给已有 Vite 样式加 `::ams::` 命名空间，避免路径碰撞
- [Fix] 小说 `#app` 上方约一行留白：入口 HTML 的 UTF-8 BOM + meta 被 Qiankun 塞进容器成为 `#app` 兄弟文本节点，按默认 `line-height`（约 21–24px）占位。去掉 BOM，宿主清理 wrapper 内 meta/空白节点，并用 `font-size:0` 兜底
- [Fix] 小说 `App.vue` 去掉裸 `.el-main` 与 `height:100vh`（改为 `100%`），避免污染主应用 `el-main` 并在嵌入态叠出视口

## [2026-08-27] qiankun-head 占位高度
- [Fix] 主应用 `index.html` + `afterMount` 隐藏 `qiankun-head`，并删掉其中的 title/meta（保留 style）
- [Fix] 小说 `el-main` 显式 `padding: 0 !important`，覆盖 Element Plus 默认 20px

## [2026-08-27] 整页滚动与样式隔离
- [Fix] 切到测试平台再回来小说样式丢失：Qiankun 沙箱摘掉 Vite 注入的 CSS 且不会重注入；`qiankunStyleKeeper` 卸载快照、再次挂载还原
- [Fix] 主应用侧栏 `100vh` 撑满且不随右侧滚动；右侧子应用宽高 100% 铺满、无外边距
- [Fix] 小说创建/详情中间区滚动，页脚吸底
- [Fix] 测试平台 `html/body/#app/.el-main` 改为 standalone 作用域，避免切换子应用时污染
- [Fix] 主应用 `.el-menu` 限定在 `.app-sider`

## [2026-08-27] 清新绿主题与分页面景观
- [UI] 主色改为清新叶绿 `#2F8A5B`；去掉琥珀金 / 魔法紫 / Element 默认蓝
- [UI] 按钮、卡片、输入、侧栏改为半透明玻璃面，景观可透出
- [UI] 列表/创建/详情分别勾勒草甸、林木、湖畔（低透明度 SVG 线稿）
- [Doc] `docs/style.md` v3；`novel-sub-style.mdc` 令牌与实现对齐

## [2026-08-27] 小说详情页
- [UI] 路由 `novels/:id`，五模块垂直 Tabs（基础信息 / 世界观 / 人物图鉴 / 篇幅大纲 / 内容组织）
- [UI] 列表看板与表格「详情」改为跳转详情页；页内「继续创作」进入向导对应步骤
- [UI] 只读展示设定 JSON：人物关系图复用 G6（`readonly`），底部进度条
- [Doc] `docs/架构关系图.md` 增补详情路由

## [2026-08-27] 小说创建向导 — 二期（交互增强）
- [UI] 步骤 3：`CharacterRelationGraph.vue`（AntV G6 人物关系网，边存 `character_edges`）
- [UI] 步骤 5：`ChapterTagList` 改用 Sortablejs 拖拽排序（替代 ↑↓）
- [Infra] 主应用 Vite 代理 `/subapps/novel-app/` → novel-sub dev（生产 entry 联调）
- [Fix] 恢复误清空的 backend / 部分 frontend 源文件

## [2026-08-26] 小说创建向导 — 阶段 7（联调与打磨）
- [UI] 创建页内容区内部滚动，底栏固定；嵌入态高度链（`App.vue`）
- [UI] 加载失败 `el-alert` + 重试 / 返回列表；API 网络错误文案优化
- [UI] 步骤条切换前自动保存当前步骤；完成/保存失败可重试提示
- [UI] 列表「继续创作」跳转 `/novels/create?id=&step=1`
- [Doc] 新增 `docs/架构关系图.md`；README 联调验收清单

## [2026-08-26] 小说创建向导 — 阶段 3～6（M2/M3）
- [UI] 步骤 2：`StepWorldSetting` + `WorldTimeline`
- [UI] 步骤 3：`CharacterLibrary` + `CharacterCard`
- [UI] 步骤 4：`OutlineTree` 卷/章组/小节
- [UI] 步骤 5：`ChapterTagList` 章节标签
- [API] `GET/PUT /api/novels/:id/setting` 分块写入 `setting_json`
- [DB] `creative_intent`、`target_audience`、`update_cadence`、`setting_json`

## [2026-08-26] 小说创建向导 — 阶段 0～2（M1）
- [UI] 路由 `novels/create`、五步壳、魔幻森林主题
- [UI] `StepBasicInfo` 基础信息 + 草稿保存
- [UI] 列表「新建小说」改路由跳转
