# fitness-agent · 站级测试清单

> 已实现测试文件一览。运行方式见 [运行命令与前置.md](./运行命令与前置.md)；fitness-agent 源码侧详 [server/tests/README.md](../../../../../../fitness-agent/server/tests/README.md)。

**Runner**：`fitness-agent/server/scripts/run-station-tests.mjs`  
**发现规则**：`frontend/tests/*` + 递归 `server/src/stations/**/tests/*.test.ts` + `server/src/platform/**/tests/*.test.ts`；s04 suite 由 `s04-pipeline.test.ts` 编排。

---

## s01 · 前端

| 文件 | filter | 需要 DB | 覆盖 |
|------|--------|---------|------|
| `frontend/tests/s01-frontend.test.mjs` | `s01` | 否 | API 导出、`vue-tsc` |
| `frontend/tests/agent-sse.unit.ts` | `agent-sse` | 否 | `formatQueueMetaHint`（B1-QUEUE-UX-001） |
| `frontend/tests/agent-sse-stream.unit.ts` | `agent-sse-stream` | 否 | `readAgentSseStream` 全事件（B1-SSE-001） |
| `frontend/tests/coach-turn-session.unit.ts` | `coach-turn-session` | 否* | 防串流 / abort（B1-STREAM-001） |

\* 需 `cd frontend && npm install`（依赖 `vue`）。

```bash
cd fitness-agent/server && npm run test:stations -- s01
cd fitness-agent/server && npm run test:stations -- agent-sse-stream
cd fitness-agent/server && npm run test:stations -- coach-turn-session
```

---

## s02 · 门禁

| 文件 | filter | 需要 DB | 覆盖 / 测试项 |
|------|--------|---------|---------------|
| `turn-submit-guard.test.ts` | `turn-submit-guard` / `s02` | 是 | coach 归属、空消息、限流、重复、在途、cancel |
| `turn-submit-guard-ext.test.ts` | `turn-submit-guard-ext` | 是 | 会员/店长 403、超长 400、结构化豁免（B2-OWN/LEN） |
| `turn-submit-guard-response.test.ts` | `guard-response` | 否 | `turnSubmitGuardToResponse` |
| `db-circuit.test.ts` | `db-circuit` | 否* | 熔断 503 + circuit_breaker（B2-CIRCUIT-001） |
| `turn-queue-policy.test.ts` | `turn-queue-policy` | 是 | warn / reject（D2-SUB-005） |
| `internal-api-key.test.ts` | `s02-key` | 否* | Internal Key 401（B2-KEY-001 / D2-SUB-006） |

\* 不依赖 Postgres 业务数据；`db-circuit` 会 import pool 但熔断路径不查库。

---

## s03 · 队列

| 文件 | filter | 需要 DB | 覆盖 / 测试项 |
|------|--------|---------|---------------|
| `turn-job-store.test.ts` | `s03` | 是 | 入队、client_turn_id 幂等 |
| `turn-job-lifecycle.test.ts` | `turn-job-lifecycle` | 是 | claim/complete/fail/stale（B3-CLAIM/STALE） |
| `turn-job-retry.test.ts` | `s03-retry` | 否 | 可重试判定、退避（B3-RETRY-001） |
| `portal-chat-session.test.ts` | `portal-chat-session` | 是 | `ensurePortalChatSession` upsert（D1-SESSION-001） |
| `turn-idempotency-poll.test.ts` | `turn-idempotency-poll` | 是 | find/poll 缓存 replay |
| `turns-router-poll.test.ts` | `turns-poll` | 是 | GET `/turns/:id` 轮询（B6-POLL-001） |

---

## s04 · Pipeline

**编排入口**：`s04-pipeline/tests/s04-pipeline.test.ts`（filter：`s04` 或 `s04-*`）

| 独立 test | filter | 需要 DB | 覆盖 |
|-----------|--------|---------|------|
| `portal-turn-pipeline-replay.test.ts` | `portal-replay` | 是 | 会员/店长幂等 replay 不调 Pi |
| `form-gates-normalize.test.ts` | `form-gates` | 是 | requireForm / pickSummaryBody |
| `turn-job-stream.test.ts` | `turn-job-stream` | 是* | stream append + thinking 节流 |

| suite 文件 | filter | 需要 DB | 覆盖 |
|------------|--------|---------|------|
| `bff-pipeline.suite.ts` | `s04-bff` | 是 | enrich、gates、memory、删除、publicId |
| `macro-plan-flow.suite.ts` | `s04-macro` | 是 | 宏观计划状态机 |
| `session-plan-flow.suite.ts` | `s04-session` | 是 | 课时状态机 |
| `outbox-contract.suite.ts` | `s04-outbox` | 否 | validateCoachOutbox 全 message_type（A1-OUTBOX-*） |
| `portal-payload.suite.ts` | `s04-portal` | 否 | validatePortalOutbox 仅 text（C2-PAYLOAD-*） |

跳过 suite：`S04_SKIP=bff,macro npm run test:stations -- s04`

---

## s05 · Pi 契约

| 文件 | filter | 需要 DB | 覆盖 |
|------|--------|---------|------|
| `s05-pi.test.ts` | `s05` | 否 | memory_ops filter、outbox 基础契约 |
| `outbox-draft-merge.test.ts` | `s05` | 否 | sidecar draft 合并 |
| `outbox-preview.test.ts` | `outbox-preview` | 否 | extractOutboxPreviewText |
| `coach-turn-fallback.test.ts` | `coach-fallback` | 否 | 无 outbox / 429 / API key 降级 |
| `memory-compress-guard.test.ts` | `memory-compress` | 否 | compress 护栏（不调 LLM） |

---

## s06 · 回传

| 文件 | filter | 需要 DB | 覆盖 |
|------|--------|---------|------|
| `turn-journey.test.ts` | `s06` | 是 | turn_journeys 六站观测 |
| `sse-frame.test.ts` | `sse-frame` | 否 | SSE 帧 status/message/done（B6-SSE-001） |
| `turn-stream-timeout.test.ts` | `sse-timeout` | 是 | streamMaxWaitMs 断流（B6-TIMEOUT-001） |

> GET 轮询见 s03 `turns-router-poll.test.ts`（B6-POLL-001 / D1-POLL-001）。

```bash
npm run test:stations -- sse-frame sse-timeout
```

---

## wiki-knowledge · 编译

| 文件 | filter | 需要 DB | 覆盖 |
|------|--------|---------|------|
| `wiki-knowledge/src/tests/wiki-compile.test.ts` | `wiki-compile` | 否 | `compileMemberPage` 计划/课时/时间线 |

```bash
npm run test:stations -- wiki-compile
```

---

## platform · 公共工具

| 文件 | filter | 需要 DB | 覆盖 |
|------|--------|---------|------|
| `platform/tests/public-id.test.ts` | `public-id` | 否 | new/parse/publicIdFromNumericId |
| `platform/tests/text-limits.test.ts` | `text-limits` | 否 | truncateUser/AssistantMessage |
| `platform/tests/member-goal-parse.test.ts` | `member-goal-parse` | 否 | 训练目标解析 / 占位 / 任务指令 |

```bash
npm run test:stations -- platform
npm run test:stations -- public-id text-limits
```

---

## 待新增（下一批）

| 计划文件 | 优先级 | 说明 |
|----------|--------|------|
| s04 member pipeline mock | P2 | mock Pi 路径 |
| s03 countQueueTurnJobs 联动 | P2 | queue policy 计数 |
| wiki compileDashboard | P2 | Dashboard 纯函数 |

完整缺口见 [单测缺口梳理.md](./单测缺口梳理.md)。
