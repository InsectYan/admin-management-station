{
  "matrix": [
    {
      "name": "缺 message 空串",
      "runner": "cli",
      "command": "cd server && npm run test:stations -- turn-submit-guard"
    },
    {
      "name": "断言：400",
      "runner": "cli",
      "command": "cd server && npm run test:stations -- turn-submit-guard",
      "_note": "同命令覆盖多断言场景，可按业务拆分 filter"
    }
  ]
}
