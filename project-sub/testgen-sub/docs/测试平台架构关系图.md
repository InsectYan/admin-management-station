# testgen-sub 测试平台架构与关系图

> 更新：2026-07-02  
> 范围：AI 智能测试平台前端交互、数据库、执行引擎、Agent 协作  
> 评分与待办 → [项目评分与后续计划.md](./项目评分与后续计划.md) · [文档索引](./README.md)

**图例**：蓝色=前端 · 绿色=BFF/引擎 · 橙色=数据库 · 紫色=Agent · 灰色=外部系统

---

## 1. 平台定位

`testgen-sub` 是 AMS 下的自包含子应用（`app_key=testgen`），承担两大核心能力：

1. **测试用例生成链路**：PRD/文档 → Agent 生成 → 写入用例库
2. **Fitness 测试体系**：383 条结构化测试项 + 10 套 TS 执行引擎 + 计划/执行/洞察

---

## 2. 整体架构图

```mermaid
flowchart TB
    classDef fe fill:#DBEAFE,stroke:#2563EB,color:#1E3A8A
    classDef bff fill:#DCFCE7,stroke:#16A34A,color:#14532D
    classDef db fill:#FEF3C7,stroke:#D97706,color:#78350F
    classDef agent fill:#F3E8FF,stroke:#9333EA,color:#581C87
    classDef ext fill:#F1F5F9,stroke:#64748B,color:#334155

    subgraph FE["前端 Vue3 5102"]
        Pages["51+ 页面"]:::fe
        Services["fitnessService / generationService"]:::fe
    end

    subgraph BFF["Egg.js BFF 5202"]
        Router["router.js"]:::bff
        Controllers["Controller 层"]:::bff
        ServicesBFF["Service 层"]:::bff
        Exec["execution Orchestrator"]:::bff
        AgentProxy["agentProxy.js"]:::bff
    end

    subgraph DB["PostgreSQL 5302"]
        Core["平台核心表"]:::db
        Fitness["Fitness 资产表"]:::db
        Runtime["运行时表 ft_run"]:::db
        Views["分析视图 12 张"]:::db
    end

    subgraph AG["Agent 平台 4001"]
        Skills["6 个 Skill 插件"]:::agent
    end

    subgraph SUT["被测系统"]
        FitnessAgent["fitness-agent BFF/CLI"]:::ext
    end

    Pages --> Services --> Router
    Router --> Controllers --> ServicesBFF
    ServicesBFF --> Core
    ServicesBFF --> Fitness
    ServicesBFF --> Runtime
    ServicesBFF --> Views
    ServicesBFF --> Exec
    Exec --> AgentProxy --> Skills
    Skills -->|internal API| Router
    Exec --> FitnessAgent
```

---

## 3. 前端页面交互关系

### 3.1 导航域划分

```mermaid
flowchart TB
    classDef root fill:#1E3A8A,stroke:#1E40AF,color:#fff
    classDef nav fill:#DBEAFE,stroke:#2563EB,color:#1E3A8A
    classDef leaf fill:#EFF6FF,stroke:#93C5FD,color:#1E40AF
    classDef warn fill:#FEF3C7,stroke:#D97706,color:#78350F

    Root["MainLayout"]:::root
    Root --> Project["项目管理"]:::nav
    Root --> Testgen["测试用例生成"]:::nav
    Root --> Config["配置管理"]:::nav
    Root --> Fitness["Fitness 测试体系"]:::nav

    Project --> PL["/projects 列表"]:::leaf
    Project --> PD["/projects/:code 详情"]:::leaf

    Testgen --> Scope["/testgen/scope"]:::leaf
    Testgen --> Items["/testgen/items 用例库"]:::leaf
    Testgen --> Jobs["/jobs/:id 进度"]:::leaf

    Config --> Tpl["/config/templates"]:::leaf
    Config --> Enum["/fitness/settings/enums"]:::leaf

    Fitness --> Dash["/fitness/dashboard"]:::leaf
    Fitness --> Assets["/fitness/assets"]:::leaf
    Fitness --> Insights["/fitness/insights"]:::leaf
    Fitness --> Plans["/fitness/plans"]:::leaf
    Fitness --> ExecNav["/fitness/execution"]:::leaf
    Fitness --> Topics["/fitness/topics 占位"]:::warn
```

### 3.2 核心业务流：用例生成

```mermaid
sequenceDiagram
    participant U as 用户
    participant Scope as TestScopePage
    participant Jobs as GenerationProgressPage
    participant BFF as testgen BFF
    participant Agent as testgen-skill
    participant DB as testgen_db

    U->>Scope: 选择项目大类 PRD 文档
    Scope->>BFF: POST /api/generation-jobs
    BFF->>Agent: invoke generate_for_fitness
    loop Loop 四步
        Agent->>BFF: POST internal agent-context
        Note over Agent: analyze functional edge review
    end
    Agent->>BFF: enrich_samples validate_draft
    BFF->>DB: 写入 generation_jobs test_item_detail
    U->>Jobs: 轮询 /jobs/:id
    Jobs->>BFF: GET /api/generation-jobs/:id
    Jobs-->>U: AgentConfigPanel 展示
```

### 3.3 核心业务流：Fitness 执行

```mermaid
sequenceDiagram
    participant U as 用户
    participant Items as FitnessItemsPage
    participant Config as FitnessRunConfigPage
    participant Launch as FitnessRunLaunchPage
    participant Console as FitnessRunConsolePage
    participant BFF as BFF Orchestrator
    participant Agent as judge sample
    participant SUT as 被测系统

    U->>Items: 筛选测试项
    U->>Config: 配置 TS Panel
    Config->>BFF: PUT /api/fitness/run-config/:itemId
    U->>Launch: 选择环境 dry-run
    Launch->>BFF: POST /api/fitness/run/:itemId/launch
    BFF->>BFF: agent_hook pre_execute
    BFF->>SUT: HttpRunner CliRunner
    BFF->>Agent: fitness-judge judge
    BFF-->>Console: SSE runs stream
    Console-->>U: pass fail 图 AI 解读
```

### 3.4 测试项详情 Tab 结构

```mermaid
flowchart LR
    classDef hub fill:#2563EB,stroke:#1E40AF,color:#fff
    classDef tab fill:#DBEAFE,stroke:#2563EB,color:#1E3A8A
    classDef ts fill:#DCFCE7,stroke:#16A34A,color:#14532D

    Item["items/:itemId"]:::hub
    Item --> Detail["详情 Tab"]:::tab
    Item --> ConfigTab["方案配置 ConfigPanel x10"]:::tab
    Item --> LaunchTab["执行确认 Tab"]:::tab
    Item --> History["执行历史 Tab"]:::tab

    ConfigTab --> Det["TS-01 DET"]:::ts
    ConfigTab --> Bnd["TS-02 BND"]:::ts
    ConfigTab --> Rep["TS-03 REP"]:::ts
    ConfigTab --> Set["TS-04 SET"]:::ts
    ConfigTab --> Chain["TS-05 CHAIN"]:::ts
    ConfigTab --> Pair["TS-06 PAIR"]:::ts
    ConfigTab --> Neg["TS-07 NEG"]:::ts
    ConfigTab --> Obs["TS-08 OBS"]:::ts
    ConfigTab --> Load["TS-09 LOAD"]:::ts
    ConfigTab --> Man["TS-10 MAN"]:::ts
```

---

## 4. 数据库表关系图

### 4.1 分层概览

```mermaid
flowchart TB
    classDef tbl fill:#fff,stroke:#94A3B8,color:#334155

    subgraph LayerA["A 平台核心 init.sql"]
        documents:::tbl
        knowledge_entries:::tbl
        modules:::tbl
        generation_jobs:::tbl
    end

    subgraph LayerB["B Fitness 资产"]
        test_item_detail:::tbl
        test_category_major:::tbl
        test_dimension:::tbl
        enums["枚举表 15 张"]:::tbl
    end

    subgraph LayerC["C 运行时"]
        test_plan:::tbl
        ft_run:::tbl
        ft_run_result:::tbl
        ft_sample_set:::tbl
    end

    subgraph LayerD["D 配置模板"]
        tpl_config_det:::tbl
        tpl_config_bnd:::tbl
        tpl_others["tpl_config 其余 8 张"]:::tbl
    end

    generation_jobs --> test_item_detail
    test_item_detail --> test_category_major
    test_item_detail --> ft_run_config["ft_run_config"]:::tbl
    ft_run --> ft_run_result
    test_plan --> test_plan_item["test_plan_item"]:::tbl
    test_plan_item --> ft_run
```

### 4.2 核心 ER 关系（简化）

```mermaid
erDiagram
    test_project ||--o{ project_env_template : has
    test_project ||--o{ generation_jobs : owns
    generation_jobs ||--o{ test_item_detail : generates
    documents ||--o{ generation_jobs : sources
    test_item_detail }o--|| test_category_major : belongs_to
    test_item_detail }o--|| test_scheme_enum : scheme
    test_item_detail ||--o| ft_run_config : configures
    ft_run_config ||--o{ ft_run : launches
    ft_run ||--o{ ft_run_result : produces
    ft_sample_set ||--o{ ft_sample_item : contains
    ft_execution_env ||--o{ ft_run : runs_on
    test_plan ||--o{ test_plan_item : includes
    test_plan_item }o--|| test_item_detail : references
    test_plan ||--o| test_plan_report : reports
```

### 4.3 表数量统计

| 分层 | 数量 | 说明 |
|------|:----:|------|
| 平台核心 | 4 | documents, knowledge, modules, generation_jobs |
| 旧链路 | 5 | env_configs, test_runs, func/perf_results |
| Fitness 种子表 | 23+ | 分类、枚举、test_item_detail（383 条） |
| 分析视图 | 12 | v_metric_*, v_analysis_*, v_risk_* |
| 运行时 | 12 | test_plan_*, ft_* |
| 配置模板 | 10 | tpl_config_* |

---

## 5. 后端 API 与执行引擎

### 5.1 API 域划分

```mermaid
flowchart LR
    classDef api fill:#DBEAFE,stroke:#2563EB,color:#1E3A8A
    classDef svc fill:#DCFCE7,stroke:#16A34A,color:#14532D
    classDef agent fill:#F3E8FF,stroke:#9333EA,color:#581C87

    P["/projects"]:::api
    G["/generation-jobs"]:::api
    F["/fitness"]:::api
    I["/internal"]:::api
    O["/test-runs 旧链路"]:::api

    P --> TestProjectSvc["testProject"]:::svc
    G --> GenerationJobSvc["generationJob"]:::svc
    GenerationJobSvc --> AgentProxyNode["agentProxy"]:::agent
    F --> FitnessSvc["fitnessAsset Plan Exec"]:::svc
    FitnessSvc --> Orchestrator["Orchestrator"]:::svc
    I --> InternalFitness["internalFitness"]:::svc
    O --> TestRunSvc["testRun"]:::svc
```

### 5.2 执行引擎架构

```mermaid
flowchart TB
    classDef entry fill:#2563EB,stroke:#1E40AF,color:#fff
    classDef core fill:#DCFCE7,stroke:#16A34A,color:#14532D
    classDef ts fill:#DBEAFE,stroke:#2563EB,color:#1E3A8A
    classDef agent fill:#F3E8FF,stroke:#9333EA,color:#581C87

    Launch["POST launch"]:::entry
    Launch --> Orch["runOrchestrator"]:::core
    Orch --> Hook["agentHook"]:::agent
    Orch --> EngReg["engineRegistry"]:::core
    EngReg --> TS01["TS-01 Det"]:::ts
    EngReg --> TS02["TS-02 Bnd"]:::ts
    EngReg --> TS03["TS-03 Rep"]:::ts
    EngReg --> TS04["TS-04 Set"]:::ts
    EngReg --> TS05["TS-05 Chain"]:::ts
    EngReg --> TS06["TS-06 Pair"]:::ts
    EngReg --> TS07["TS-07 Neg"]:::ts
    EngReg --> TS08["TS-08 Obs"]:::ts
    EngReg --> TS09["TS-09 Load"]:::ts
    EngReg --> TS10["TS-10 Man"]:::ts
    Orch --> VSReg["vsRegistry"]:::core
    VSReg --> VS["vsContract vsRate vsAgentJudge"]:::core
    Orch --> Runners["http cli matrix journey"]:::core
    Hook --> AgentProxyNode["agentProxy"]:::agent
    VSReg --> AgentProxyNode
```

### 5.3 Agent 代理调用关系

```mermaid
flowchart LR
    classDef hub fill:#9333EA,stroke:#6B21A8,color:#fff
    classDef skill fill:#F3E8FF,stroke:#9333EA,color:#581C87
    classDef target fill:#DCFCE7,stroke:#16A34A,color:#14532D

    BFF["agentProxy.js"]:::hub
    BFF --> S1["testgen-skill"]:::skill
    BFF --> S2["fitness-judge-skill"]:::skill
    BFF --> S3["fitness-sample-skill"]:::skill
    BFF --> S4["fitness-config-skill"]:::skill
    BFF --> S5["fitness-explore-skill"]:::skill
    BFF --> S6["perf-bottleneck-skill"]:::skill

    S1 --> T1["internal fitness items"]:::target
    S2 --> T2["vsAgentJudge TS-10"]:::target
    S3 --> T3["TS-04 pre_execute"]:::target
    S4 --> T4["ConfigPanel AI"]:::target
    S5 --> T5["TS-05 explore"]:::target
    S6 --> T6["suite 性能报告"]:::target
```

---

## 6. 52 大类与 10 套 TS 模板映射

| 模板 | TS 方案 | 关联 Skill | 测试平台页面 |
|------|---------|------------|--------------|
| TPL-DET | TS-01 确定性单次 | fitness-config-skill | ConfigDetPanel |
| TPL-BND | TS-02 边界矩阵 | fitness-config + fitness-sample | ConfigBndPanel |
| TPL-REP | TS-03 重复抽样 | fitness-config-skill | ConfigRepPanel |
| TPL-SET | TS-04 固定样本集 | fitness-sample-skill | ConfigSetPanel |
| TPL-CHAIN | TS-05 多步链路 | fitness-config + fitness-explore | ConfigChainPanel |
| TPL-PAIR | TS-06 对照对比 | fitness-config-skill | ConfigPairPanel |
| TPL-NEG | TS-07 对抗专项 | fitness-sample-skill | ConfigNegPanel |
| TPL-OBS | TS-08 可观测稽核 | fitness-config-skill | ConfigObsPanel |
| TPL-LOAD | TS-09 压测容量 | fitness-config + perf-bottleneck | ConfigLoadPanel |
| TPL-MAN | TS-10 人工评审 | fitness-judge-skill | ConfigManPanel |

详细大类映射见 [设计-配置模板与52大类.md](./设计-配置模板与52大类.md)

---

## 7. 相关文档索引

| 文档 | 路径 |
|------|------|
| **文档索引** | [README.md](./README.md) |
| **项目评分与后续计划** | [项目评分与后续计划.md](./项目评分与后续计划.md) |
| 配置模板设计 | [设计-配置模板与52大类.md](./设计-配置模板与52大类.md) |
| Agent 协作设计 | [设计-Agent协作与Skill嵌入.md](./设计-Agent协作与Skill嵌入.md) |
| Agent 联调 | [设计-Agent联调配置.md](./设计-Agent联调配置.md) |
| 待开发节点 | [待办-开发节点追踪.md](./待办-开发节点追踪.md) |
| Agent 任务清单 | [待办-Agent开发任务清单.md](./待办-Agent开发任务清单.md) |
| 启动说明 | [../README.md](../README.md) |
| AMS 平台关系 | [../../docs-project/AMS平台架构关系图.md](../../docs-project/AMS平台架构关系图.md) |
