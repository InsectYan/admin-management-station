# v_analysis_scheme_validation_matrix · TS×VS 方案矩阵

> **类型**：分析类视图

主方案与主验证标准组合用例数

## 说明

- `init.sql`：`CREATE OR REPLACE VIEW` 定义（需在基表导入后执行）
- `data.json`：基于当前 **383** 条测试项的**快照样例**（非入库数据，供报表/前端对接参考）

## 样例行数

**26** 行（生成时计算）

## 依赖

```sql
SELECT * FROM v_analysis_scheme_validation_matrix;
```
