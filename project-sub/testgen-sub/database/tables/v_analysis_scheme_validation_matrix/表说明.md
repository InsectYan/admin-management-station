# v_analysis_scheme_validation_matrix · TS×VS 组合矩阵

> 主 TS 与主 VS 组合的用例计数。

共 **26** 条样例。

## 表用途

方案百科与计划范围分析。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `scheme_id` | — | 计算列 | scheme_id | — |
| `scheme_name` | — | 计算列 | scheme_name | — |
| `validation_id` | — | 计算列 | validation_id | — |
| `validation_name` | — | 计算列 | validation_name | — |
| `item_count` | — | 计算列 | item_count | — |

## 视图说明

- **类型**：分析类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_analysis_scheme_validation_matrix;
```
