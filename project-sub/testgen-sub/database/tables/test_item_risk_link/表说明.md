# test_item_risk_link · 风险项↔主项关联

> 66 条 canonical 方向 risk → main。

共 **66** 条。

## 表用途

存储 E/C2/C3 高风险清单与 F5 症状项对主链用例的 GUARD/DETECT 关系；双向查询用视图 `v_risk_link_bidirectional`。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `link_id` | VARCHAR(16) | PK | 关联编码 | 主键 |
| `risk_item_id` | VARCHAR(64) | NOT NULL FK→test_item_detail | 风险项 | FK → test_item_detail，风险/症状项（E-RISK-* 等） |
| `main_item_id` | VARCHAR(64) | NOT NULL FK→test_item_detail | 主项 | FK → test_item_detail，被防护/验证的主链项 |
| `relation_type_id` | VARCHAR(16) | NOT NULL FK→test_item_relation_type_enum | 关系类型 | FK → test_item_relation_type_enum |
| `risk_category` | VARCHAR(16) | NOT NULL | 风险分类 | 风险分类标签 |
| `is_primary` | BOOLEAN | NOT NULL | is_primary | 是否主防护项 |
| `is_bidirectional` | BOOLEAN | NOT NULL | is_bidirectional | — |
| `description` | TEXT | — | 说明 | — |

## 关联关系

### 本表引用（外键）

| 本表字段 | 关联表 | 关联字段 | 关系 |
|----------|--------|----------|------|
| `risk_item_id` | `test_item_detail` | `item_id` | N:1 |
| `main_item_id` | `test_item_detail` | `item_id` | N:1 |
| `relation_type_id` | `test_item_relation_type_enum` | `relation_type_id` | N:1 |

## 数据文件

| 文件 | 说明 |
|------|------|
| `init.sql` | DDL + INSERT |
| `data.json` | 冗余备份/展示 enrich 字段 |
