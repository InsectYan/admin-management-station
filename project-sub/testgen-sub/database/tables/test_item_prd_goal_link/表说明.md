# test_item_prd_goal_link · 测试项↔PRD目标

> 262 条多对多链接。

共 **262** 条。

## 表用途

规范化 PRD 目标覆盖统计；一条用例可覆盖多个 G0x 目标。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `link_id` | VARCHAR(16) | PK | link_id | 主键 |
| `item_id` | VARCHAR(64) | NOT NULL FK→test_item_detail | item_id | FK → test_item_detail |
| `prd_goal_id` | VARCHAR(8) | NOT NULL FK→prd_goal | prd_goal_id | FK → prd_goal |
| `is_primary` | BOOLEAN | — | is_primary | — |

## 关联关系

### 本表引用（外键）

| 本表字段 | 关联表 | 关联字段 | 关系 |
|----------|--------|----------|------|
| `item_id` | `test_item_detail` | `item_id` | N:1 |
| `prd_goal_id` | `prd_goal` | `prd_goal_id` | N:1 |

## 数据文件

| 文件 | 说明 |
|------|------|
| `init.sql` | DDL + INSERT |
| `data.json` | 冗余备份/展示 enrich 字段 |
