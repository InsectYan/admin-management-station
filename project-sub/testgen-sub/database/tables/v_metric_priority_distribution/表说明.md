# v_metric_priority_distribution · 优先级分布

> P0–P3 用例数量与占比。

共 **4** 条样例。

## 表用途

计划优先级结构分析。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `priority_id` | — | 计算列 | priority_id | — |
| `priority_name` | — | 计算列 | priority_name | — |
| `item_count` | — | 计算列 | item_count | — |
| `item_id) / NULLIF((SELECT COUNT(*)` | — | 计算列 | item_id) / NULLIF((SELECT COUNT(*) | — |

## 视图说明

- **类型**：指标类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_metric_priority_distribution;
```
