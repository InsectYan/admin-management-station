# test_scheme_validation_pair · TS↔VS 推荐配对

> 18 条推荐主/辅 TS 与 VS 配对。

共 **18** 条。

## 表用途

文档级推荐组合，供计划向导与方案百科参考；测试项实际绑定以 item 四列 TS/VS 为准。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `pair_id` | VARCHAR(64) | PK | 配对编码 | 主键，P01–P18 |
| `scheme_id` | VARCHAR(64) | FK→test_scheme_enum | 方案 | FK → test_scheme_enum |
| `validation_id` | VARCHAR(64) | FK→test_validation_enum | 验证 | FK → test_validation_enum |
| `scheme_name` | TEXT | — | 方案名称 | — |
| `validation_name` | TEXT | — | 验证名称 | — |
| `is_primary` | BOOLEAN | NOT NULL | 主配对 | 是否推荐为主配对 |

## 关联关系

### 本表引用（外键）

| 本表字段 | 关联表 | 关联字段 | 关系 |
|----------|--------|----------|------|
| `scheme_id` | `test_scheme_enum` | `scheme_id` | N:1 |
| `validation_id` | `test_validation_enum` | `validation_id` | N:1 |

## 数据文件

| 文件 | 说明 |
|------|------|
| `init.sql` | DDL + INSERT |
| `data.json` | 冗余备份/展示 enrich 字段 |
