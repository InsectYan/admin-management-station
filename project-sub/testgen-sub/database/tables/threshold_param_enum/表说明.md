# threshold_param_enum · 阈值参数占位

> 14 项发版阈值占位符（rate_M、passk_N、block_H…）。

共 **14** 条。

## 表用途

关联 VS 统计类验证；**具体数值在测试计划填写，不入库**。计划报告对比时使用。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `param_id` | VARCHAR(64) | PK | 参数编码 | 主键，如 rate_M、passk_N |
| `validation_id` | VARCHAR(64) | FK→test_validation_enum | 验证标准 | FK → test_validation_enum，所属 VS 类型 |
| `name` | TEXT | — | 参数名称 | — |
| `unit` | TEXT | — | 单位 | — |
| `placeholder` | TEXT | — | 占位符 | 发版计划填写占位说明 |

## 关联关系

### 本表引用（外键）

| 本表字段 | 关联表 | 关联字段 | 关系 |
|----------|--------|----------|------|
| `validation_id` | `test_validation_enum` | `validation_id` | N:1 |

## 数据文件

| 文件 | 说明 |
|------|------|
| `init.sql` | DDL + INSERT |
| `data.json` | 冗余备份/展示 enrich 字段 |
