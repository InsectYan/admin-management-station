# v_metric_dimension_summary · 维度指标汇总

> 各维度用例数、P0 数、自动化覆盖率等。

共 **8** 条样例。

## 表用途

指标中心 / 仪表盘维度 Tab；基于 `test_dimension` LEFT JOIN `test_item_detail` 聚合。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `dimension_id` | — | 计算列 | 维度编码 | — |
| `dimension_name` | — | 计算列 | 维度名称 | — |
| `total_items` | — | 计算列 | 用例总数 | — |
| `item_count / NULLIF((SELECT COUNT(*)` | — | 计算列 | item_count / NULLIF((SELECT COUNT(*) | — |

## 视图说明

- **类型**：指标类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_metric_dimension_summary;
```
