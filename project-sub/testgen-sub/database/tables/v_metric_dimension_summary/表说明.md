# v_metric_dimension_summary · 维度指标汇总

> **类型**：指标类视图

各维度用例数、P0、自动化覆盖率、可观测项数

## 说明

- `init.sql`：`CREATE OR REPLACE VIEW` 定义（需在基表导入后执行）
- `data.json`：基于当前 **383** 条测试项的**快照样例**（非入库数据，供报表/前端对接参考）

## 样例行数

**8** 行（生成时计算）

## 依赖

```sql
SELECT * FROM v_metric_dimension_summary;
```
