# v_risk_link_bidirectional · 风险双向关联

> **类型**：关联类视图

RISK_TO_MAIN 与 MAIN_TO_RISK 统一视图

## 说明

- `init.sql`：`CREATE OR REPLACE VIEW` 定义（需在基表导入后执行）
- `data.json`：基于当前 **383** 条测试项的**快照样例**（非入库数据，供报表/前端对接参考）

## 样例行数

**132** 行（生成时计算）

## 依赖

```sql
SELECT * FROM v_risk_link_bidirectional;
```
