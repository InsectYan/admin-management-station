# v_risk_link_bidirectional · 风险双向关联视图

> 将 risk_link 展开为 RISK_TO_MAIN 与 MAIN_TO_RISK 双向行。

共 **132** 条样例。

## 表用途

测试项详情「风险关系」Tab；支持从主项或风险项双向浏览图谱。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `direction` | — | 计算列 | 方向 | — |
| `source_item_id` | — | 计算列 | 源用例 | — |
| `target_item_id` | — | 计算列 | 目标用例 | — |
| `relation_type_id` | — | 计算列 | 关系类型 | — |
| `risk_category` | — | 计算列 | risk_category | — |
| `is_primary` | — | 计算列 | is_primary | — |
| `description` | — | 计算列 | description | — |
| `link_id` | — | 计算列 | link_id | — |

## 视图说明

- **类型**：关联类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_risk_link_bidirectional;
```
