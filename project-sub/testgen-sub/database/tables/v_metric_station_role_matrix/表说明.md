# v_metric_station_role_matrix · 六站×三端矩阵

> 站点与角色交叉的用例计数。

共 **12** 条样例。

## 表用途

覆盖矩阵热力图数据源。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `station_id` | — | 计算列 | station_id | — |
| `role_scope_id` | — | 计算列 | role_scope_id | — |
| `item_count` | — | 计算列 | item_count | — |

## 视图说明

- **类型**：指标类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_metric_station_role_matrix;
```
