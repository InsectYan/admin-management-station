# test_category_minor_scheme · 子类默认方案映射

> 88 条子类 → 默认 TS/VS，来源于 scheme-map.json。

共 **88** 条。

## 表用途

在 `test_item_prefix_scheme` 未命中时，按子类为测试项补全主/辅方案与验证标准；供 `resolve-item-schemes.js` 注入使用。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `category_minor_id` | VARCHAR(64) | PK FK→test_category_minor | 子类编码 | PK/FK → test_category_minor |
| `item_prefix` | VARCHAR(64) | NOT NULL | ID前缀 | 对应 test_item_id 前缀，如 C1-MACRO- |
| `scheme_primary_id` | VARCHAR(16) | FK→test_scheme_enum | 主方案 | FK → test_scheme_enum |
| `scheme_secondary_id` | VARCHAR(16) | FK→test_scheme_enum | 辅方案 | FK → test_scheme_enum，可选辅 TS |
| `validation_primary_id` | VARCHAR(20) | FK→test_validation_enum | 主验证 | FK → test_validation_enum |
| `validation_secondary_id` | VARCHAR(20) | FK→test_validation_enum | 辅验证 | FK → test_validation_enum，可选辅 VS |
| `sample_execution_note` | TEXT | — | 执行说明 | — |
| `mapping_source` | VARCHAR(128) | — | 映射来源 | scheme-map.json 或 dimension/major-default |

## 关联关系

### 本表引用（外键）

| 本表字段 | 关联表 | 关联字段 | 关系 |
|----------|--------|----------|------|
| `category_minor_id` | `test_category_minor` | `category_minor_id` | N:1 |
| `scheme_primary_id` | `test_scheme_enum` | `scheme_id` | N:1 |
| `scheme_secondary_id` | `test_scheme_enum` | `scheme_id` | N:1 |
| `validation_primary_id` | `test_validation_enum` | `validation_id` | N:1 |
| `validation_secondary_id` | `test_validation_enum` | `validation_id` | N:1 |

## 数据文件

| 文件 | 说明 |
|------|------|
| `init.sql` | DDL |
| `data.json` | 种子数据（db-cli 注入） |
