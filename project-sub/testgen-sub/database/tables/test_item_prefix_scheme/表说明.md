# test_item_prefix_scheme · 测试项前缀方案规则

> 168 条 item_id 前缀 → TS/VS 最长前缀匹配规则。

共 **168** 条。

## 表用途

从 A–H `*-方案映射.md` 解析；优先级高于 scheme-map 与大类/维度默认。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `mapping_id` | VARCHAR(16) | PK | 映射编码 | 主键 MAP0001… |
| `item_prefix` | VARCHAR(64) | NOT NULL | 前缀 | 最长前缀匹配键；可精确 ID 或 C1-MACRO- 形式 |
| `scheme_primary_id` | VARCHAR(16) | FK→test_scheme_enum | 主方案 | FK → test_scheme_enum |
| `scheme_secondary_id` | VARCHAR(16) | FK→test_scheme_enum | scheme_secondary_id | FK → test_scheme_enum |
| `validation_primary_id` | VARCHAR(20) | FK→test_validation_enum | 主验证 | FK → test_validation_enum |
| `validation_secondary_id` | VARCHAR(20) | FK→test_validation_enum | validation_secondary_id | FK → test_validation_enum |
| `sample_execution_note` | TEXT | — | sample_execution_note | — |
| `mapping_source` | VARCHAR(128) | — | 来源 | 来源 *-方案映射.md 文件名 |

## 关联关系

### 本表引用（外键）

| 本表字段 | 关联表 | 关联字段 | 关系 |
|----------|--------|----------|------|
| `scheme_primary_id` | `test_scheme_enum` | `scheme_id` | N:1 |
| `scheme_secondary_id` | `test_scheme_enum` | `scheme_id` | N:1 |
| `validation_primary_id` | `test_validation_enum` | `validation_id` | N:1 |
| `validation_secondary_id` | `test_validation_enum` | `validation_id` | N:1 |

## 数据文件

| 文件 | 说明 |
|------|------|
| `init.sql` | DDL + INSERT |
| `data.json` | 冗余备份/展示 enrich 字段 |
