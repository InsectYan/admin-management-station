# v_analysis_risk_gap · 风险覆盖缺口

> **类型**：分析类视图

COVERAGE 为 GAP/PARTIAL 的风险项

## 说明

- `init.sql`：`CREATE OR REPLACE VIEW` 定义（需在基表导入后执行）
- `data.json`：基于当前 **383** 条测试项的**快照样例**（非入库数据，供报表/前端对接参考）

## 样例行数

**11** 行（生成时计算）

## 依赖

```sql
SELECT * FROM v_analysis_risk_gap;
```
