# v_analysis_risk_gap · 风险覆盖缺口

> 无 GUARD 或 PARTIAL 的高风险项清单。

共 **11** 条样例。

## 表用途

风险中心缺口 Tab、发版就绪输入。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `*` | — | 计算列 | * | — |

## 视图说明

- **类型**：分析类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_analysis_risk_gap;
```
