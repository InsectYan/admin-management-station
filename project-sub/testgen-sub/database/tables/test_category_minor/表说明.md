# test_category_minor · 测试子类

> 88 个标准子类 key（如 C1_MACRO、B2_GUARD），归属大类。

共 **88** 条。

## 表用途

细粒度业务/技术节划分；`test_item_detail.category_minor_id` 由 item_id 解析写入。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `category_minor_id` | VARCHAR(64) | PK | 子类编码 | 主键，如 C1_MACRO、B2_GUARD |
| `category_major_id` | VARCHAR(64) | FK→test_category_major | 大类 | FK → test_category_major |
| `name` | TEXT | — | 子类名称 | — |
| `sort_order` | SMALLINT | — | 排序 | 同大类内展示排序 |

## 关联关系

### 本表引用（外键）

| 本表字段 | 关联表 | 关联字段 | 关系 |
|----------|--------|----------|------|
| `category_major_id` | `test_category_major` | `category_major_id` | N:1 |

### 被其他表引用

| 引用表 | 外键字段 | 关系 |
|--------|----------|------|
| `test_category_minor_scheme` | `category_minor_id` | 1:N |
| `test_item_detail` | `category_minor_id` | 1:N |

## 数据文件

| 文件 | 说明 |
|------|------|
| `init.sql` | DDL + INSERT |
| `data.json` | 冗余备份/展示 enrich 字段 |
