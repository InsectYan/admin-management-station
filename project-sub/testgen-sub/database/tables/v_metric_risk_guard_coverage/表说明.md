# v_metric_risk_guard_coverage · 风险防护覆盖率

> 高风险项 GUARD 链接 COVERED/PARTIAL/GAP。

共 **32** 条样例。

## 表用途

风险中心指标 Tab。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `risk_item_id` | — | 计算列 | risk_item_id | — |
| `risk_name` | — | 计算列 | risk_name | — |
| `risk_category` | — | 计算列 | risk_category | — |
| `priority_id` | — | 计算列 | priority_id | — |
| `guard_link_count` | — | 计算列 | guard_link_count | — |
| `primary_guard_count` | — | 计算列 | primary_guard_count | — |
| `has_primary_guard` | — | 计算列 | has_primary_guard | — |
| `coverage_status` | — | 计算列 | coverage_status | — |

## 视图说明

- **类型**：指标类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_metric_risk_guard_coverage;
```
