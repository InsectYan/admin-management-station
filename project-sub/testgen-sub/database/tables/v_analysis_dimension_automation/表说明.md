# v_analysis_dimension_automation · 维度自动化就绪

> 各维度 AUTO_TODO 占比与就绪率。

共 **8** 条样例。

## 表用途

分析中心自动化 Tab。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `dimension_id` | — | 计算列 | dimension_id | — |
| `total` | — | 计算列 | total | — |
| `existing` | — | 计算列 | existing | — |
| `partial` | — | 计算列 | partial | — |
| `todo` | — | 计算列 | todo | — |
| `manual` | — | 计算列 | manual | — |
| `0 * COUNT(*) FILTER (WHERE automation_status_id <> 'AUTO_TODO') / NULLIF(COUNT(*)` | — | 计算列 | 0 * COUNT(*) FILTER (WHERE automation_status_id <> 'AUTO_TODO') / NULLIF(COUNT(*) | — |
| `0)` | — | 计算列 | 0) | — |
| `automation_ready_pct` | — | 计算列 | automation_ready_pct | — |

## 视图说明

- **类型**：分析类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_analysis_dimension_automation;
```
