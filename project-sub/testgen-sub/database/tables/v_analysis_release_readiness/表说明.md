# v_analysis_release_readiness · 发版就绪分析

> **类型**：分析类视图

P0待建、风险缺口、PRD缺口、GREEN/YELLOW/RED 信号

## 说明

- `init.sql`：`CREATE OR REPLACE VIEW` 定义（需在基表导入后执行）
- `data.json`：基于当前 **383** 条测试项的**快照样例**（非入库数据，供报表/前端对接参考）

## 样例行数

**1** 行（生成时计算）

## 依赖

```sql
SELECT * FROM v_analysis_release_readiness;
```
