# v_analysis_release_readiness · 发版就绪分析

> 单行 GREEN/YELLOW/RED 发版信号。

共 **1** 条样例。

## 表用途

综合 P0 待建自动化、风险缺口、PRD 缺口生成发版建议。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `(SELECT COUNT(*)` | — | 计算列 | (SELECT COUNT(*) | — |

## 视图说明

- **类型**：分析类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_analysis_release_readiness;
```
