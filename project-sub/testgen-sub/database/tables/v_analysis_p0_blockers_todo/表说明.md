# v_analysis_p0_blockers_todo · P0 阻塞待建

> **类型**：分析类视图

P0 且 automation=AUTO_TODO 的用例清单

## 说明

- `init.sql`：`CREATE OR REPLACE VIEW` 定义（需在基表导入后执行）
- `data.json`：基于当前 **383** 条测试项的**快照样例**（非入库数据，供报表/前端对接参考）

## 样例行数

**89** 行（生成时计算）

## 依赖

```sql
SELECT * FROM v_analysis_p0_blockers_todo;
```
