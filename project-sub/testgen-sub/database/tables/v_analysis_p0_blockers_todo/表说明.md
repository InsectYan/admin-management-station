# v_analysis_p0_blockers_todo · P0待建自动化清单

> is_p0_blocker 且 AUTO_TODO 的用例列表。

共 **89** 条样例。

## 表用途

发版阻塞项清单，可跳转执行配置。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `item_id` | — | 计算列 | item_id | — |
| `item_name` | — | 计算列 | item_name | — |
| `dimension_id` | — | 计算列 | dimension_id | — |
| `category_major_id` | — | 计算列 | category_major_id | — |
| `scheme_primary_id` | — | 计算列 | scheme_primary_id | — |
| `validation_primary_id` | — | 计算列 | validation_primary_id | — |
| `automation_entry_id` | — | 计算列 | automation_entry_id | — |
| `is_risk_flag` | — | 计算列 | is_risk_flag | — |

## 视图说明

- **类型**：分析类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_analysis_p0_blockers_todo;
```
