# test_category_major · 测试大类

> 52 个大类（A1、C1、B2、E_RISK…），归属维度并携带默认 TS/VS。

共 **52** 条。

## 表用途

连接维度与子类的中层分类。`item_id` 前缀通常含大类段（如 C1-MACRO-001 的 C1）。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `category_major_id` | VARCHAR(64) | PK | 大类编码 | 主键，如 A1、C1、B2、E_RISK |
| `dimension_id` | VARCHAR(64) | FK→test_dimension | 维度 | FK → test_dimension |
| `name` | TEXT | — | 大类名称 | — |
| `description` | TEXT | — | 说明 | — |
| `item_count` | TEXT | — | 用例数 | 该大类下用例数 |
| `default_scheme_id` | VARCHAR(64) | — | 默认方案 | FK → test_scheme_enum，大类默认主 TS |

## 关联关系

### 本表引用（外键）

| 本表字段 | 关联表 | 关联字段 | 关系 |
|----------|--------|----------|------|
| `dimension_id` | `test_dimension` | `dimension_id` | N:1 |

### 被其他表引用

| 引用表 | 外键字段 | 关系 |
|--------|----------|------|
| `test_category_minor` | `category_major_id` | 1:N |
| `test_item_detail` | `category_major_id` | 1:N |

## 数据文件

| 文件 | 说明 |
|------|------|
| `init.sql` | DDL + INSERT |
| `data.json` | 冗余备份/展示 enrich 字段 |
