# config_env_enum · 配置项枚举

> 26 项架构 appSettings / 门禁 / 队列相关配置 key。

共 **26** 条。

## 表用途

规范化 `test_item_detail.config_env_id`；压测与门禁用例常引用 TURN_*、DB_CIRCUIT_* 等。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `config_env_id` | VARCHAR(64) | PK | 配置编码 | 主键，如 TURN_QUEUE_MAX_PENDING |
| `name` | TEXT | — | 配置名称 | — |
| `domain` | TEXT | — | 域 | 配置域：guard | queue | pipeline | memory 等 |
| `default_value` | TEXT | — | 默认值 | 文档默认值或占位说明 |

## 关联关系


### 被其他表引用

| 引用表 | 外键字段 | 关系 |
|--------|----------|------|
| `test_item_detail` | `config_env_id` | 1:N |

## 数据文件

| 文件 | 说明 |
|------|------|
| `init.sql` | DDL + INSERT |
| `data.json` | 冗余备份/展示 enrich 字段 |
