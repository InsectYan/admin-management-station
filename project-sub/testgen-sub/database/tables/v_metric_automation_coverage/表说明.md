# v_metric_automation_coverage · 自动化覆盖率

> 按自动化状态统计占比。

共 **4** 条样例。

## 表用途

自动化建设进度总览。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `automation_status_id` | — | 计算列 | automation_status_id | — |
| `status_name` | — | 计算列 | status_name | — |
| `item_count` | — | 计算列 | item_count | — |
| `item_id) / NULLIF((SELECT COUNT(*)` | — | 计算列 | item_id) / NULLIF((SELECT COUNT(*) | — |

## 视图说明

- **类型**：指标类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_metric_automation_coverage;
```
