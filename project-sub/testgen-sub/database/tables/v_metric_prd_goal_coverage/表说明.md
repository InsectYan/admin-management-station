# v_metric_prd_goal_coverage · PRD 目标覆盖

> **类型**：指标类视图

G01–G10 关联用例数与覆盖等级 OK/LOW/GAP

## 说明

- `init.sql`：`CREATE OR REPLACE VIEW` 定义（需在基表导入后执行）
- `data.json`：基于当前 **383** 条测试项的**快照样例**（非入库数据，供报表/前端对接参考）

## 样例行数

**10** 行（生成时计算）

## 依赖

```sql
SELECT * FROM v_metric_prd_goal_coverage;
```
