# v_metric_prd_goal_coverage · PRD目标覆盖率

> G01–G10 每项链接用例数与 OK/LOW/GAP 状态。

共 **10** 条样例。

## 表用途

PRD 十条目标覆盖看板；JOIN `test_item_prd_goal_link`。

## 字段说明

| 字段 | 类型 | 约束 | 中文名 | 说明 |
|------|------|------|--------|------|
| `prd_goal_id` | — | 计算列 | prd_goal_id | — |
| `goal_no` | — | 计算列 | goal_no | — |
| `goal_name` | — | 计算列 | goal_name | — |
| `linked_item_count` | — | 计算列 | linked_item_count | — |
| `linked_p0_count` | — | 计算列 | linked_p0_count | — |
| `coverage_note` | — | 计算列 | coverage_note | — |

## 视图说明

- **类型**：指标类视图
- **依赖**：基表数据导入后由 `ams-testgen db` 自动 CREATE OR REPLACE
- **data.json**：快照样例，供前端/报表对接参考，不参与 seed

```sql
SELECT * FROM v_metric_prd_goal_coverage;
```
